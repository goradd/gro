// Code generated by goradd-orm. DO NOT EDIT.

package goradd_unit

import (
	"bytes"
	"context"
	"encoding/gob"
	"encoding/json"
	"strconv"
	"testing"

	"github.com/goradd/orm/_test/gen/orm/goradd_unit/node"
	"github.com/goradd/orm/pkg/op"
	"github.com/goradd/orm/pkg/test"
	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
)

// createMinimalSampleLeafNl creates an unsaved minimal version of a LeafNl object
// for testing.
func createMinimalSampleLeafNl() *LeafNl {
	obj := NewLeafNl()
	updateMinimalSampleLeafNl(obj)

	return obj
}

// updateMinimalSampleLeafNl sets the values of a minimal sample to new, random values.
func updateMinimalSampleLeafNl(obj *LeafNl) {

	obj.SetName(test.RandomValue[string](100))

}

// createMaximalSampleLeafNl creates an unsaved version of a LeafNl object
// for testing that includes references to minimal objects.
func createMaximalSampleLeafNl(ctx context.Context) *LeafNl {
	obj := NewLeafNl()
	updateMaximalSampleLeafNl(ctx, obj)
	return obj
}

// updateMaximalSampleLeafNl sets all the maximal sample values to new values.
// This will set new values for references, so save the old values and delete them.
func updateMaximalSampleLeafNl(ctx context.Context, obj *LeafNl) {
	updateMinimalSampleLeafNl(obj)
	obj.SetRootNl(createMinimalSampleRootNl())

	obj.SetLeaf2s(createMinimalSampleLeafNl())
	obj.SetLeaf1s(createMinimalSampleLeafNl())
}

// deleteSampleLeafNl deletes an object created and saved by one of the sample creator functions.
func deleteSampleLeafNl(ctx context.Context, obj *LeafNl) {
	if obj == nil {
		return
	}

	for _, item := range obj.Leaf2s() {
		deleteSampleLeafNl(ctx, item)
	}
	for _, item := range obj.Leaf1s() {
		deleteSampleLeafNl(ctx, item)
	}

	_ = obj.Delete(ctx)
	deleteSampleRootNl(ctx, obj.RootNl())
}

// assertEqualFieldsLeafNl compares two objects and asserts that the basic fields are equal.
func assertEqualFieldsLeafNl(t *testing.T, obj1, obj2 *LeafNl) {
	if obj1.IDIsLoaded() && obj2.IDIsLoaded() { // only check loaded values
		assert.EqualValues(t, obj1.ID(), obj2.ID())
	}
	if obj1.NameIsLoaded() && obj2.NameIsLoaded() { // only check loaded values
		assert.EqualValues(t, obj1.Name(), obj2.Name())
	}
	if obj1.GroLockIsLoaded() && obj2.GroLockIsLoaded() { // only check loaded values
		assert.EqualValues(t, obj1.GroLock(), obj2.GroLock())
	}

}

func TestLeafNl_SetID(t *testing.T) {

	obj := NewLeafNl()

	assert.True(t, obj.IsNew())
	val := test.RandomNumberString()
	obj.SetID(val)
	assert.Equal(t, val, obj.ID())

	// test default
	obj.SetID("")
	assert.EqualValues(t, "", obj.ID(), "set default")

	// test panic on setting value larger than maximum size allowed
	val = test.RandomValue[string](33)
	assert.Panics(t, func() {
		obj.SetID(val)
	})
}
func TestLeafNl_SetName(t *testing.T) {

	obj := NewLeafNl()

	assert.True(t, obj.IsNew())
	val := test.RandomValue[string](100)
	obj.SetName(val)
	assert.Equal(t, val, obj.Name())

	// test default
	obj.SetName("")
	assert.EqualValues(t, "", obj.Name(), "set default")

	// test panic on setting value larger than maximum size allowed
	val = test.RandomValue[string](101)
	assert.Panics(t, func() {
		obj.SetName(val)
	})
}
func TestLeafNl_SetRootNlID(t *testing.T) {

	obj := NewLeafNl()

	assert.True(t, obj.IsNew())
	val := test.RandomValue[string](32)
	obj.SetRootNlID(val)
	assert.Equal(t, val, obj.RootNlID())
	assert.False(t, obj.RootNlIDIsNull())

	// Test NULL
	obj.SetRootNlIDToNull()
	assert.EqualValues(t, "", obj.RootNlID())
	assert.True(t, obj.RootNlIDIsNull())

	// test default
	obj.SetRootNlID("")
	assert.EqualValues(t, "", obj.RootNlID(), "set default")

	// test panic on setting value larger than maximum size allowed
	val = test.RandomValue[string](33)
	assert.Panics(t, func() {
		obj.SetRootNlID(val)
	})
}

func TestLeafNl_Copy(t *testing.T) {
	obj := createMinimalSampleLeafNl()

	obj2 := obj.Copy()

	assert.Equal(t, obj.Name(), obj2.Name())
	assert.Equal(t, obj.RootNlID(), obj2.RootNlID())

}

func TestLeafNl_BasicInsert(t *testing.T) {
	obj := createMinimalSampleLeafNl()
	ctx := context.Background()
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleLeafNl(ctx, obj)

	// Test retrieval
	obj2, err := LoadLeafNl(ctx, obj.PrimaryKey())
	require.NotNil(t, obj2)
	assert.NoError(t, err)

	assert.Equal(t, obj2.PrimaryKey(), obj2.OriginalPrimaryKey())

	assert.True(t, obj2.IDIsLoaded())
	assert.Panics(t, func() {
		obj2.SetID(obj2.ID())
	})

	assert.True(t, obj2.NameIsLoaded())
	// test that setting it to the same value will not change the dirty bit
	assert.False(t, obj2.nameIsDirty)
	obj2.SetName(obj2.Name())
	assert.False(t, obj2.nameIsDirty)

	assert.True(t, obj2.GroLockIsLoaded())

}

func TestLeafNl_InsertPanics(t *testing.T) {
	obj := createMinimalSampleLeafNl()
	_ = obj
	ctx := context.Background()
	_ = ctx

	obj.rootNl = nil

	obj.nameIsLoaded = false
	assert.Panics(t, func() { obj.Save(ctx) })
	obj.nameIsLoaded = true

}

func TestLeafNl_BasicUpdate(t *testing.T) {
	obj := createMinimalSampleLeafNl()
	ctx := context.Background()
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleLeafNl(ctx, obj)
	updateMinimalSampleLeafNl(obj)
	assert.NoError(t, obj.Save(ctx))
	obj2, err := LoadLeafNl(ctx, obj.PrimaryKey())
	assert.NoError(t, err)

	assert.Equal(t, obj2.ID(), obj.ID(), "ID did not update")
	assert.Equal(t, obj2.Name(), obj.Name(), "Name did not update")
	assert.Equal(t, obj2.GroLock(), obj.GroLock(), "GroLock did not update")
}

func TestLeafNl_ReferenceLoad(t *testing.T) {
	ctx := context.Background()
	obj := createMaximalSampleLeafNl(ctx)
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleLeafNl(ctx, obj)

	// Test that referenced objects were saved and assigned ids
	assert.NotNil(t, obj.RootNl())
	assert.NotEqual(t, '-', obj.RootNl().PrimaryKey()[0])

	// Test lazy loading
	obj2, err := LoadLeafNl(ctx, obj.PrimaryKey())
	assert.NoError(t, err)
	assert.NotNil(t, obj2)
	objPkOnly, err2 := LoadLeafNl(ctx, obj.PrimaryKey(),
		node.LeafNl().ID())
	assert.NoError(t, err2)
	_ = objPkOnly

	assert.Nil(t, obj2.RootNl(), "RootNl is not loaded initially")
	v_RootNl, _ := obj2.LoadRootNl(ctx)
	assert.NotNil(t, v_RootNl)
	assert.Equal(t, v_RootNl.PrimaryKey(), obj2.RootNl().PrimaryKey())
	assert.Equal(t, obj.RootNl().PrimaryKey(), obj2.RootNl().PrimaryKey())
	assert.True(t, obj2.RootNlIDIsLoaded())

	assert.False(t, objPkOnly.RootNlIDIsLoaded())
	assert.Panics(t, func() { _, _ = objPkOnly.LoadRootNl(ctx) })

	assert.Nil(t, obj2.Leaf2s(), "Leaf2s is not loaded initially")
	v_Leaf2s, _ := obj2.LoadLeaf2s(ctx)
	assert.NotNil(t, v_Leaf2s)
	assert.Len(t, v_Leaf2s, 1)
	assert.Nil(t, obj2.Leaf1s(), "Leaf1s is not loaded initially")
	v_Leaf1s, _ := obj2.LoadLeaf1s(ctx)
	assert.NotNil(t, v_Leaf1s)
	assert.Len(t, v_Leaf1s, 1)

	// test eager loading
	obj3, _ := LoadLeafNl(ctx, obj.PrimaryKey(), node.LeafNl().RootNl(),
		node.LeafNl().Leaf2s(),
		node.LeafNl().Leaf1s(),
	)
	_ = obj3 // avoid error if there are no references

	assert.Equal(t, obj2.RootNl().PrimaryKey(), obj3.RootNl().PrimaryKey())
	assert.Equal(t, len(obj2.Leaf2s()), len(obj3.Leaf2s()))
	assert.Equal(t, len(obj2.Leaf1s()), len(obj3.Leaf1s()))

}

func TestLeafNl_ReferenceUpdateNewObjects(t *testing.T) {
	ctx := context.Background()
	obj := createMaximalSampleLeafNl(ctx)
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleLeafNl(ctx, obj)

	obj2, _ := LoadLeafNl(ctx, obj.PrimaryKey())
	updateMaximalSampleLeafNl(ctx, obj2)
	assert.NoError(t, obj2.Save(ctx))
	defer deleteSampleLeafNl(ctx, obj2)

	obj3, _ := LoadLeafNl(ctx, obj2.PrimaryKey(), node.LeafNl().RootNl(),
		node.LeafNl().Leaf2s(),
		node.LeafNl().Leaf1s(),
	)
	_ = obj3 // avoid error if there are no references

	assert.Equal(t, obj2.RootNl().PrimaryKey(), obj3.RootNl().PrimaryKey())

	assert.Equal(t, len(obj2.Leaf2s()), len(obj3.Leaf2s()))
	assert.Equal(t, len(obj2.Leaf1s()), len(obj3.Leaf1s()))

}

func TestLeafNl_ReferenceUpdateOldObjects(t *testing.T) {
	ctx := context.Background()
	obj := createMaximalSampleLeafNl(ctx)
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleLeafNl(ctx, obj)

	updateMinimalSampleRootNl(obj.RootNl())
	updateMinimalSampleLeafNl(obj.Leaf2s()[0])
	updateMinimalSampleLeafNl(obj.Leaf1s()[0])

	assert.NoError(t, obj.Save(ctx))

	obj2, _ := LoadLeafNl(ctx, obj.PrimaryKey(), node.LeafNl().RootNl(),
		node.LeafNl().Leaf2s(),
		node.LeafNl().Leaf1s(),
	)
	_ = obj2 // avoid error if there are no references

	assertEqualFieldsRootNl(t, obj2.RootNl(), obj.RootNl())

	assertEqualFieldsLeafNl(t, obj2.Leaf2s()[0], obj.Leaf2s()[0])
	assertEqualFieldsLeafNl(t, obj2.Leaf1s()[0], obj.Leaf1s()[0])
}
func TestLeafNl_EmptyPrimaryKeyGetter(t *testing.T) {
	obj := NewLeafNl()

	i, err := strconv.Atoi(obj.ID())
	assert.NoError(t, err)
	assert.True(t, i < 0)
}

func TestLeafNl_Getters(t *testing.T) {
	obj := createMinimalSampleLeafNl()

	i, err := strconv.Atoi(obj.ID())
	assert.NoError(t, err)
	assert.True(t, i < 0)

	ctx := context.Background()
	require.NoError(t, obj.Save(ctx))
	defer deleteSampleLeafNl(ctx, obj)

	has, _ := HasLeafNl(ctx, obj.PrimaryKey())
	assert.True(t, has)

	obj2, _ := LoadLeafNl(ctx, obj.PrimaryKey(),
		node.LeafNl().ID())

	assert.Equal(t, obj.ID(), obj.Get(LeafNlIDField))
	assert.Equal(t, obj.Name(), obj.Get(LeafNlNameField))
	assert.Panics(t, func() { obj2.Name() })
	assert.Nil(t, obj2.Get(LeafNlNameField))
	assert.Equal(t, obj.GroLock(), obj.Get(LeafNlGroLockField))
	assert.Panics(t, func() { obj2.GroLock() })
	assert.Nil(t, obj2.Get(LeafNlGroLockField))
	// Not loaded
	assert.Nil(t, obj2.RootNl())
	assert.Nil(t, obj2.Get(LeafNlRootNlField))
	assert.Panics(t, func() { obj2.RootNlID() })
	assert.Nil(t, obj2.Get(LeafNlRootNlIDField))

}

func TestLeafNl_QueryLoad(t *testing.T) {
	obj := createMinimalSampleLeafNl()
	ctx := context.Background()
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleLeafNl(ctx, obj)

	objs, err := QueryLeafNls(ctx).
		Where(op.Equal(node.LeafNl().ID(), obj.ID())).
		OrderBy(node.LeafNl().ID()). // exercise order by
		Limit(1, 0).                 // exercise limit
		Calculation(node.LeafNl(), "IsTrue", op.Equal("A", "A")).
		Load()
	assert.NoError(t, err)
	assert.Equal(t, obj.PrimaryKey(), objs[0].PrimaryKey())
	assert.True(t, objs[0].GetAlias("IsTrue").Bool())
}
func TestLeafNl_QueryLoadI(t *testing.T) {
	obj := createMinimalSampleLeafNl()
	ctx := context.Background()
	err := obj.Save(ctx)
	assert.NoError(t, err)
	defer deleteSampleLeafNl(ctx, obj)

	objs, _ := QueryLeafNls(ctx).
		Where(op.Equal(node.LeafNl().ID(), obj.ID())).
		LoadI()

	assert.Equal(t, obj.PrimaryKey(), objs[0].(*LeafNl).PrimaryKey())
}
func TestLeafNl_QueryCursor(t *testing.T) {
	obj := createMinimalSampleLeafNl()
	ctx := context.Background()
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleLeafNl(ctx, obj)

	cursor, err := QueryLeafNls(ctx).
		Where(op.Equal(node.LeafNl().ID(), obj.ID())).
		LoadCursor()
	require.NoError(t, err)
	obj2, err2 := cursor.Next()
	assert.Equal(t, obj.PrimaryKey(), obj2.PrimaryKey())
	require.NoError(t, err2)
	obj2, err2 = cursor.Next()
	assert.Nil(t, obj2)
	require.NoError(t, err2)
	assert.NoError(t, cursor.Close())

	// test empty cursor result
	cursor, err = QueryLeafNls(ctx).
		Where(op.Equal("B", "A")).
		LoadCursor()
	require.NoError(t, err)

	obj2, err = cursor.Next()
	assert.Nil(t, obj2)
	require.NoError(t, err)
	assert.NoError(t, cursor.Close())
}
func TestLeafNl_Count(t *testing.T) {
	ctx := context.Background()
	obj := createMaximalSampleLeafNl(ctx)
	err := obj.Save(ctx)
	assert.NoError(t, err)
	defer deleteSampleLeafNl(ctx, obj)
	assert.Positive(t, func() int { i, _ := CountLeafNls(ctx); return i }())

	// reread in case there are data limitations imposed by the database
	obj2, _ := LoadLeafNl(ctx, obj.PrimaryKey())
	assert.Positive(t,
		func() int {
			i, _ := CountLeafNlsByRootNlID(ctx,
				obj2.RootNlID())
			return i
		}())

}

func TestLeafNl_MarshalJSON(t *testing.T) {
	obj := createMinimalSampleLeafNl()

	b, err := json.Marshal(obj)
	assert.NoError(t, err)

	obj2 := NewLeafNl()
	err = json.Unmarshal(b, &obj2)
	assert.NoError(t, err)

	assertEqualFieldsLeafNl(t, obj, obj2)
}

func TestLeafNl_MarshalBinary(t *testing.T) {
	obj := createMinimalSampleLeafNl()

	b, err := obj.MarshalBinary()
	assert.NoError(t, err)

	obj2 := NewLeafNl()
	err = obj2.UnmarshalBinary(b)
	assert.NoError(t, err)

	assertEqualFieldsLeafNl(t, obj, obj2)
}

func TestLeafNl_FailingMarshalBinary(t *testing.T) {
	obj := createMinimalSampleLeafNl()
	var err error

	for i := 0; i < 15; i++ {
		enc := &test.GobEncoder{Count: i}
		err = obj.encodeTo(enc)
		assert.Error(t, err)
	}
	// do it again with aliases
	obj._aliases = make(map[string]any)
	for i := 0; i < 16; i++ {
		enc := &test.GobEncoder{Count: i}
		err = obj.encodeTo(enc)
		assert.Error(t, err)
	}
}

func TestLeafNl_FailingUnmarshalBinary(t *testing.T) {
	obj := createMinimalSampleLeafNl()
	b, err := obj.MarshalBinary()
	assert.NoError(t, err)
	obj2 := NewLeafNl()
	for i := 0; i < 15; i++ {
		buf := bytes.NewReader(b)
		dec := &test.GobDecoder{Decoder: gob.NewDecoder(buf), Count: i}
		err = obj2.decodeFrom(dec)
		assert.Error(t, err)
	}

	// do it again with aliases
	obj = createMinimalSampleLeafNl()
	obj._aliases = map[string]any{"a": 1}
	b, err = obj.MarshalBinary()
	assert.NoError(t, err)

	obj2 = NewLeafNl()
	for i := 0; i < 16; i++ {
		buf := bytes.NewReader(b)
		dec := &test.GobDecoder{Decoder: gob.NewDecoder(buf), Count: i}
		err = obj2.decodeFrom(dec)
		assert.Error(t, err)
	}
}
