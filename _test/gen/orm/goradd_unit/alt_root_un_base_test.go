// Code generated by goradd-orm. DO NOT EDIT.

package goradd_unit

import (
	"bytes"
	"context"
	"encoding/gob"
	"encoding/json"
	"testing"

	"github.com/goradd/gro/_test/gen/orm/goradd_unit/node"
	"github.com/goradd/gro/pkg/op"
	"github.com/goradd/gro/pkg/test"
	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
)

// createMinimalSampleAltRootUn creates an unsaved minimal version of a AltRootUn object
// for testing.
func createMinimalSampleAltRootUn() *AltRootUn {
	obj := NewAltRootUn()
	updateMinimalSampleAltRootUn(obj)

	obj.SetID(test.RandomValue[float32](32))

	return obj
}

// updateMinimalSampleAltRootUn sets the values of a minimal sample to new, random values.
func updateMinimalSampleAltRootUn(obj *AltRootUn) {

	obj.SetName(test.RandomValue[string](100))

}

// createMaximalSampleAltRootUn creates an unsaved version of a AltRootUn object
// for testing that includes references to minimal objects.
func createMaximalSampleAltRootUn(ctx context.Context) *AltRootUn {
	obj := NewAltRootUn()
	obj.SetID(test.RandomValue[float32](32))
	updateMaximalSampleAltRootUn(ctx, obj)
	return obj
}

// updateMaximalSampleAltRootUn sets all the maximal sample values to new values.
// This will set new values for references, so save the old values and delete them.
func updateMaximalSampleAltRootUn(ctx context.Context, obj *AltRootUn) {
	updateMinimalSampleAltRootUn(obj)

	{
		obj2, _ := obj.LoadAltLeafUn(ctx)
		// only update if not already set, since it can't be changed once set unless the reverse object is deleted first.
		if obj2 == nil {
			obj.SetAltLeafUn(createMinimalSampleAltLeafUn())
		}
	}
}

// deleteSampleAltRootUn deletes an object created and saved by one of the sample creator functions.
func deleteSampleAltRootUn(ctx context.Context, obj *AltRootUn) {
	if obj == nil {
		return
	}

	deleteSampleAltLeafUn(ctx, obj.AltLeafUn())

	_ = obj.Delete(ctx)
}

// assertEqualFieldsAltRootUn compares two objects and asserts that the basic fields are equal.
func assertEqualFieldsAltRootUn(t *testing.T, obj1, obj2 *AltRootUn) {
	if obj1.IDIsLoaded() && obj2.IDIsLoaded() { // only check loaded values
		assert.EqualValues(t, obj1.ID(), obj2.ID())
	}
	if obj1.NameIsLoaded() && obj2.NameIsLoaded() { // only check loaded values
		assert.EqualValues(t, obj1.Name(), obj2.Name())
	}

}

func TestAltRootUn_SetID(t *testing.T) {

	obj := NewAltRootUn()

	assert.True(t, obj.IsNew())
	val := test.RandomValue[float32](32)
	obj.SetID(val)
	assert.Equal(t, val, obj.ID())

	// test default
	var d float32 = 0
	obj.SetID(d)
	assert.EqualValues(t, d, obj.ID(), "set default")

}
func TestAltRootUn_SetName(t *testing.T) {

	obj := NewAltRootUn()

	assert.True(t, obj.IsNew())
	val := test.RandomValue[string](100)
	obj.SetName(val)
	assert.Equal(t, val, obj.Name())

	// test default
	var d string = ""
	obj.SetName(d)
	assert.EqualValues(t, d, obj.Name(), "set default")

	// test panic on setting value larger than maximum size allowed
	val = test.RandomValue[string](101)
	assert.Panics(t, func() {
		obj.SetName(val)
	})
}

func TestAltRootUn_Copy(t *testing.T) {
	obj := createMinimalSampleAltRootUn()

	obj2 := obj.Copy()

	assert.Equal(t, obj.ID(), obj2.ID())
	assert.Equal(t, obj.Name(), obj2.Name())

}

func TestAltRootUn_BasicInsert(t *testing.T) {
	obj := createMinimalSampleAltRootUn()
	ctx := context.Background()
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleAltRootUn(ctx, obj)

	// Test retrieval
	obj2, err := LoadAltRootUn(ctx, obj.PrimaryKey())
	require.NotNil(t, obj2)
	assert.NoError(t, err)

	assert.Equal(t, obj2.PrimaryKey(), obj2.OriginalPrimaryKey())

	assert.True(t, obj2.IDIsLoaded())
	assert.Panics(t, func() {
		obj2.SetID(obj2.ID())
	})

	assert.True(t, obj2.NameIsLoaded())
	// test that setting it to the same value will not change the dirty bit
	assert.False(t, obj2.nameIsDirty)
	obj2.SetName(obj2.Name())
	assert.False(t, obj2.nameIsDirty)

}

func TestAltRootUn_InsertPanics(t *testing.T) {
	obj := createMinimalSampleAltRootUn()
	_ = obj
	ctx := context.Background()
	_ = ctx

	obj.idIsLoaded = false
	assert.Panics(t, func() { obj.Save(ctx) })
	obj.idIsLoaded = true

	obj.nameIsLoaded = false
	assert.Panics(t, func() { obj.Save(ctx) })
	obj.nameIsLoaded = true

}

func TestAltRootUn_BasicUpdate(t *testing.T) {
	obj := createMinimalSampleAltRootUn()
	ctx := context.Background()
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleAltRootUn(ctx, obj)
	updateMinimalSampleAltRootUn(obj)
	assert.NoError(t, obj.Save(ctx))
	obj2, err := LoadAltRootUn(ctx, obj.PrimaryKey())
	assert.NoError(t, err)

	assert.Equal(t, obj2.ID(), obj.ID(), "ID did not update")
	assert.Equal(t, obj2.Name(), obj.Name(), "Name did not update")
}

func TestAltRootUn_ReferenceLoad(t *testing.T) {
	ctx := context.Background()
	obj := createMaximalSampleAltRootUn(ctx)
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleAltRootUn(ctx, obj)

	// Test that referenced objects were saved and assigned ids

	// Test lazy loading
	obj2, err := LoadAltRootUn(ctx, obj.PrimaryKey())
	assert.NoError(t, err)
	assert.NotNil(t, obj2)
	objPkOnly, err2 := LoadAltRootUn(ctx, obj.PrimaryKey(),
		node.AltRootUn().ID())
	assert.NoError(t, err2)
	_ = objPkOnly

	assert.Nil(t, obj2.AltLeafUn(), "AltLeafUn is not loaded initially")
	v_AltLeafUn, _ := obj2.LoadAltLeafUn(ctx)
	assert.NotNil(t, v_AltLeafUn)
	assert.Equal(t, v_AltLeafUn.PrimaryKey(), obj2.AltLeafUn().PrimaryKey())
	assert.Equal(t, obj.AltLeafUn().PrimaryKey(), obj2.AltLeafUn().PrimaryKey())

	// test eager loading
	obj3, err3 := LoadAltRootUn(ctx, obj.PrimaryKey(), node.AltRootUn().AltLeafUn())
	assert.NoError(t, err3)
	_ = obj3 // avoid error if there are no references

	assert.Equal(t, obj2.AltLeafUn().PrimaryKey(), obj3.AltLeafUn().PrimaryKey())

}

func TestAltRootUn_ReferenceUpdateNewObjects(t *testing.T) {
	ctx := context.Background()
	obj := createMaximalSampleAltRootUn(ctx)
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleAltRootUn(ctx, obj)

	obj2, err := LoadAltRootUn(ctx, obj.PrimaryKey())
	assert.NoError(t, err)
	updateMaximalSampleAltRootUn(ctx, obj2)
	assert.NoError(t, obj2.Save(ctx))
	defer deleteSampleAltRootUn(ctx, obj2)

	obj3, err2 := LoadAltRootUn(ctx, obj2.PrimaryKey(), node.AltRootUn().AltLeafUn())
	assert.NoError(t, err2)
	_ = obj3 // avoid error if there are no references

	assert.Equal(t, obj2.AltLeafUn().PrimaryKey(), obj3.AltLeafUn().PrimaryKey())

}

func TestAltRootUn_ReferenceUpdateOldObjects(t *testing.T) {
	ctx := context.Background()
	obj := createMaximalSampleAltRootUn(ctx)
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleAltRootUn(ctx, obj)

	updateMinimalSampleAltLeafUn(obj.AltLeafUn())

	assert.NoError(t, obj.Save(ctx))

	obj2, err := LoadAltRootUn(ctx, obj.PrimaryKey(), node.AltRootUn().AltLeafUn())
	assert.NoError(t, err)
	_ = obj2 // avoid error if there are no references

	assertEqualFieldsAltLeafUn(t, obj2.AltLeafUn(), obj.AltLeafUn())

}
func TestAltRootUn_EmptyPrimaryKeyGetter(t *testing.T) {
	obj := NewAltRootUn()

	assert.Zero(t, obj.ID())
}

func TestAltRootUn_Getters(t *testing.T) {
	obj := createMinimalSampleAltRootUn()

	ctx := context.Background()
	require.NoError(t, obj.Save(ctx))
	defer deleteSampleAltRootUn(ctx, obj)

	has, _ := HasAltRootUn(ctx, obj.PrimaryKey())
	assert.True(t, has)

	obj2, _ := LoadAltRootUn(ctx, obj.PrimaryKey(),
		node.AltRootUn().ID())
	assert.Equal(t, obj.PrimaryKey(), obj2.PrimaryKey())

	assert.Equal(t, obj.ID(), obj.Get(AltRootUnIDField))
	assert.Equal(t, obj.Name(), obj.Get(AltRootUnNameField))
	assert.Panics(t, func() { obj2.Name() })
	assert.Nil(t, obj2.Get(AltRootUnNameField))

}

func TestAltRootUn_QueryLoad(t *testing.T) {
	obj := createMinimalSampleAltRootUn()
	ctx := context.Background()
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleAltRootUn(ctx, obj)

	objs, err := QueryAltRootUns(ctx).
		Where(op.Equal(node.AltRootUn().ID(), obj.ID())).
		OrderBy(node.AltRootUn().ID()). // exercise order by
		Limit(1, 0).                    // exercise limit
		Calculation(node.AltRootUn(), "IsTrue", op.Equal("A", "A")).
		Load()
	assert.NoError(t, err)
	assert.Equal(t, obj.PrimaryKey(), objs[0].PrimaryKey())
	assert.True(t, objs[0].GetAlias("IsTrue").Bool())
}
func TestAltRootUn_QueryLoadI(t *testing.T) {
	obj := createMinimalSampleAltRootUn()
	ctx := context.Background()
	err := obj.Save(ctx)
	assert.NoError(t, err)
	defer deleteSampleAltRootUn(ctx, obj)

	objs, _ := QueryAltRootUns(ctx).
		Where(op.Equal(node.AltRootUn().ID(), obj.ID())).
		LoadI()

	assert.Equal(t, obj.PrimaryKey(), objs[0].(*AltRootUn).PrimaryKey())
}
func TestAltRootUn_QueryCursor(t *testing.T) {
	obj := createMinimalSampleAltRootUn()
	ctx := context.Background()
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleAltRootUn(ctx, obj)

	cursor, err := QueryAltRootUns(ctx).
		Where(op.Equal(node.AltRootUn().ID(), obj.ID())).
		LoadCursor()
	require.NoError(t, err)
	obj2, err2 := cursor.Next()
	assert.Equal(t, obj.PrimaryKey(), obj2.PrimaryKey())
	require.NoError(t, err2)
	obj2, err2 = cursor.Next()
	assert.Nil(t, obj2)
	require.NoError(t, err2)
	assert.NoError(t, cursor.Close())

	// test empty cursor result
	cursor, err = QueryAltRootUns(ctx).
		Where(op.Equal("B", "A")).
		LoadCursor()
	require.NoError(t, err)

	obj2, err = cursor.Next()
	assert.Nil(t, obj2)
	require.NoError(t, err)
	assert.NoError(t, cursor.Close())
}
func TestAltRootUn_Count(t *testing.T) {
	ctx := context.Background()
	obj := createMaximalSampleAltRootUn(ctx)
	err := obj.Save(ctx)
	assert.NoError(t, err)
	defer deleteSampleAltRootUn(ctx, obj)
	assert.Positive(t, func() int { i, _ := CountAltRootUns(ctx); return i }())

}

func TestAltRootUn_MarshalJSON(t *testing.T) {
	obj := createMinimalSampleAltRootUn()

	b, err := json.Marshal(obj)
	assert.NoError(t, err)

	obj2 := NewAltRootUn()
	err = json.Unmarshal(b, &obj2)
	assert.NoError(t, err)

	assertEqualFieldsAltRootUn(t, obj, obj2)
}

func TestAltRootUn_MarshalBinary(t *testing.T) {
	obj := createMinimalSampleAltRootUn()

	b, err := obj.MarshalBinary()
	assert.NoError(t, err)

	obj2 := NewAltRootUn()
	err = obj2.UnmarshalBinary(b)
	assert.NoError(t, err)

	assertEqualFieldsAltRootUn(t, obj, obj2)
}

func TestAltRootUn_FailingMarshalBinary(t *testing.T) {
	obj := createMinimalSampleAltRootUn()
	var err error

	for i := 0; i < 11; i++ {
		enc := &test.GobEncoder{Count: i}
		err = obj.encodeTo(enc)
		assert.Error(t, err)
	}
	// do it again with aliases
	obj._aliases = make(map[string]any)
	for i := 0; i < 12; i++ {
		enc := &test.GobEncoder{Count: i}
		err = obj.encodeTo(enc)
		assert.Error(t, err)
	}
}

func TestAltRootUn_FailingUnmarshalBinary(t *testing.T) {
	obj := createMinimalSampleAltRootUn()
	b, err := obj.MarshalBinary()
	assert.NoError(t, err)
	obj2 := NewAltRootUn()
	for i := 0; i < 11; i++ {
		buf := bytes.NewReader(b)
		dec := &test.GobDecoder{Decoder: gob.NewDecoder(buf), Count: i}
		err = obj2.decodeFrom(dec)
		assert.Error(t, err)
	}

	// do it again with aliases
	obj = createMinimalSampleAltRootUn()
	obj._aliases = map[string]any{"a": 1}
	b, err = obj.MarshalBinary()
	assert.NoError(t, err)

	obj2 = NewAltRootUn()
	for i := 0; i < 12; i++ {
		buf := bytes.NewReader(b)
		dec := &test.GobDecoder{Decoder: gob.NewDecoder(buf), Count: i}
		err = obj2.decodeFrom(dec)
		assert.Error(t, err)
	}
}
