// Code generated by goradd-orm. DO NOT EDIT.

package goradd_unit

import (
	"bytes"
	"context"
	"encoding/gob"
	"encoding/json"
	"strconv"
	"testing"

	"github.com/goradd/gro/_test/gen/orm/goradd_unit/node"
	"github.com/goradd/gro/pkg/op"
	"github.com/goradd/gro/pkg/query"
	"github.com/goradd/gro/pkg/test"
	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
)

// createMinimalSampleRoot creates an unsaved minimal version of a Root object
// for testing.
func createMinimalSampleRoot() *Root {
	obj := NewRoot()
	updateMinimalSampleRoot(obj)

	return obj
}

// updateMinimalSampleRoot sets the values of a minimal sample to new, random values.
func updateMinimalSampleRoot(obj *Root) {

	obj.SetName(test.RandomValue[string](100))

}

// createMaximalSampleRoot creates an unsaved version of a Root object
// for testing that includes references to minimal objects.
func createMaximalSampleRoot(ctx context.Context) *Root {
	obj := NewRoot()
	updateMaximalSampleRoot(ctx, obj)
	return obj
}

// updateMaximalSampleRoot sets all the maximal sample values to new values.
// This will set new values for references, so save the old values and delete them.
func updateMaximalSampleRoot(ctx context.Context, obj *Root) {
	updateMinimalSampleRoot(obj)

	obj.SetLeafs(createMinimalSampleLeaf())
}

// deleteSampleRoot deletes an object created and saved by one of the sample creator functions.
func deleteSampleRoot(ctx context.Context, obj *Root) {
	if obj == nil {
		return
	}

	for _, item := range obj.Leafs() {
		deleteSampleLeaf(ctx, item)
	}

	_ = obj.Delete(ctx)
}

// assertEqualFieldsRoot compares two objects and asserts that the basic fields are equal.
func assertEqualFieldsRoot(t *testing.T, obj1, obj2 *Root) {
	if obj1.IDIsLoaded() && obj2.IDIsLoaded() { // only check loaded values
		assert.EqualValues(t, obj1.ID(), obj2.ID())
	}
	if obj1.NameIsLoaded() && obj2.NameIsLoaded() { // only check loaded values
		assert.EqualValues(t, obj1.Name(), obj2.Name())
	}

}

func TestRoot_SetID(t *testing.T) {

	obj := NewRoot()

	assert.True(t, obj.IsNew())
	val := test.RandomNumberString()
	obj.SetID(val)
	assert.Equal(t, val, obj.ID())

	// test default
	obj.SetID(query.TempAutoPrimaryKey())
	assert.EqualValues(t, query.TempAutoPrimaryKey(), obj.ID(), "set default")

}
func TestRoot_SetName(t *testing.T) {

	obj := NewRoot()

	assert.True(t, obj.IsNew())
	val := test.RandomValue[string](100)
	obj.SetName(val)
	assert.Equal(t, val, obj.Name())

	// test default
	obj.SetName("")
	assert.EqualValues(t, "", obj.Name(), "set default")

	// test panic on setting value larger than maximum size allowed
	val = test.RandomValue[string](101)
	assert.Panics(t, func() {
		obj.SetName(val)
	})
}

func TestRoot_Copy(t *testing.T) {
	obj := createMinimalSampleRoot()

	obj2 := obj.Copy()

	assert.Equal(t, obj.Name(), obj2.Name())

}

func TestRoot_BasicInsert(t *testing.T) {
	obj := createMinimalSampleRoot()
	ctx := context.Background()
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleRoot(ctx, obj)

	// Test retrieval
	obj2, err := LoadRoot(ctx, obj.PrimaryKey())
	require.NotNil(t, obj2)
	assert.NoError(t, err)

	assert.Equal(t, obj2.PrimaryKey(), obj2.OriginalPrimaryKey())

	assert.True(t, obj2.IDIsLoaded())
	assert.Panics(t, func() {
		obj2.SetID(obj2.ID())
	})

	assert.True(t, obj2.NameIsLoaded())
	// test that setting it to the same value will not change the dirty bit
	assert.False(t, obj2.nameIsDirty)
	obj2.SetName(obj2.Name())
	assert.False(t, obj2.nameIsDirty)

}

func TestRoot_InsertPanics(t *testing.T) {
	obj := createMinimalSampleRoot()
	_ = obj
	ctx := context.Background()
	_ = ctx

	obj.nameIsLoaded = false
	assert.Panics(t, func() { obj.Save(ctx) })
	obj.nameIsLoaded = true

}

func TestRoot_BasicUpdate(t *testing.T) {
	obj := createMinimalSampleRoot()
	ctx := context.Background()
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleRoot(ctx, obj)
	updateMinimalSampleRoot(obj)
	assert.NoError(t, obj.Save(ctx))
	obj2, err := LoadRoot(ctx, obj.PrimaryKey())
	assert.NoError(t, err)

	assert.Equal(t, obj2.ID(), obj.ID(), "ID did not update")
	assert.Equal(t, obj2.Name(), obj.Name(), "Name did not update")
}

func TestRoot_ReferenceLoad(t *testing.T) {
	ctx := context.Background()
	obj := createMaximalSampleRoot(ctx)
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleRoot(ctx, obj)

	// Test that referenced objects were saved and assigned ids

	// Test lazy loading
	obj2, err := LoadRoot(ctx, obj.PrimaryKey())
	assert.NoError(t, err)
	assert.NotNil(t, obj2)
	objPkOnly, err2 := LoadRoot(ctx, obj.PrimaryKey(),
		node.Root().ID())
	assert.NoError(t, err2)
	_ = objPkOnly

	assert.Nil(t, obj2.Leafs(), "Leaf is not loaded initially")
	v_Leafs, _ := obj2.LoadLeafs(ctx)
	assert.NotNil(t, v_Leafs)
	assert.Len(t, v_Leafs, 1)

	// test eager loading
	obj3, _ := LoadRoot(ctx, obj.PrimaryKey(), node.Root().Leafs())
	_ = obj3 // avoid error if there are no references

	assert.Equal(t, len(obj2.Leafs()), len(obj3.Leafs()))

}

func TestRoot_ReferenceUpdateNewObjects(t *testing.T) {
	ctx := context.Background()
	obj := createMaximalSampleRoot(ctx)
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleRoot(ctx, obj)

	obj2, _ := LoadRoot(ctx, obj.PrimaryKey())
	updateMaximalSampleRoot(ctx, obj2)
	assert.NoError(t, obj2.Save(ctx))
	defer deleteSampleRoot(ctx, obj2)

	obj3, _ := LoadRoot(ctx, obj2.PrimaryKey(), node.Root().Leafs())
	_ = obj3 // avoid error if there are no references

	assert.Equal(t, len(obj2.Leafs()), len(obj3.Leafs()))

}

func TestRoot_ReferenceUpdateOldObjects(t *testing.T) {
	ctx := context.Background()
	obj := createMaximalSampleRoot(ctx)
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleRoot(ctx, obj)

	updateMinimalSampleLeaf(obj.Leafs()[0])

	assert.NoError(t, obj.Save(ctx))

	obj2, _ := LoadRoot(ctx, obj.PrimaryKey(), node.Root().Leafs())
	_ = obj2 // avoid error if there are no references

	assertEqualFieldsLeaf(t, obj2.Leafs()[0], obj.Leafs()[0])

}
func TestRoot_EmptyPrimaryKeyGetter(t *testing.T) {
	obj := NewRoot()

	i, err := strconv.Atoi(obj.ID())
	assert.NoError(t, err)
	assert.True(t, i < 0)
}

func TestRoot_Getters(t *testing.T) {
	obj := createMinimalSampleRoot()

	i, err := strconv.Atoi(obj.ID())
	assert.NoError(t, err)
	assert.True(t, i < 0)

	ctx := context.Background()
	require.NoError(t, obj.Save(ctx))
	defer deleteSampleRoot(ctx, obj)

	has, _ := HasRoot(ctx, obj.PrimaryKey())
	assert.True(t, has)

	obj2, _ := LoadRoot(ctx, obj.PrimaryKey(),
		node.Root().ID())

	assert.Equal(t, obj.ID(), obj.Get(RootIDField))
	assert.Equal(t, obj.Name(), obj.Get(RootNameField))
	assert.Panics(t, func() { obj2.Name() })
	assert.Nil(t, obj2.Get(RootNameField))

}

func TestRoot_QueryLoad(t *testing.T) {
	obj := createMinimalSampleRoot()
	ctx := context.Background()
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleRoot(ctx, obj)

	objs, err := QueryRoots(ctx).
		Where(op.Equal(node.Root().ID(), obj.ID())).
		OrderBy(node.Root().ID()). // exercise order by
		Limit(1, 0).               // exercise limit
		Calculation(node.Root(), "IsTrue", op.Equal("A", "A")).
		Load()
	assert.NoError(t, err)
	assert.Equal(t, obj.PrimaryKey(), objs[0].PrimaryKey())
	assert.True(t, objs[0].GetAlias("IsTrue").Bool())
}
func TestRoot_QueryLoadI(t *testing.T) {
	obj := createMinimalSampleRoot()
	ctx := context.Background()
	err := obj.Save(ctx)
	assert.NoError(t, err)
	defer deleteSampleRoot(ctx, obj)

	objs, _ := QueryRoots(ctx).
		Where(op.Equal(node.Root().ID(), obj.ID())).
		LoadI()

	assert.Equal(t, obj.PrimaryKey(), objs[0].(*Root).PrimaryKey())
}
func TestRoot_QueryCursor(t *testing.T) {
	obj := createMinimalSampleRoot()
	ctx := context.Background()
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleRoot(ctx, obj)

	cursor, err := QueryRoots(ctx).
		Where(op.Equal(node.Root().ID(), obj.ID())).
		LoadCursor()
	require.NoError(t, err)
	obj2, err2 := cursor.Next()
	assert.Equal(t, obj.PrimaryKey(), obj2.PrimaryKey())
	require.NoError(t, err2)
	obj2, err2 = cursor.Next()
	assert.Nil(t, obj2)
	require.NoError(t, err2)
	assert.NoError(t, cursor.Close())

	// test empty cursor result
	cursor, err = QueryRoots(ctx).
		Where(op.Equal("B", "A")).
		LoadCursor()
	require.NoError(t, err)

	obj2, err = cursor.Next()
	assert.Nil(t, obj2)
	require.NoError(t, err)
	assert.NoError(t, cursor.Close())
}
func TestRoot_Count(t *testing.T) {
	ctx := context.Background()
	obj := createMaximalSampleRoot(ctx)
	err := obj.Save(ctx)
	assert.NoError(t, err)
	defer deleteSampleRoot(ctx, obj)
	assert.Positive(t, func() int { i, _ := CountRoots(ctx); return i }())

}

func TestRoot_MarshalJSON(t *testing.T) {
	obj := createMinimalSampleRoot()

	b, err := json.Marshal(obj)
	assert.NoError(t, err)

	obj2 := NewRoot()
	err = json.Unmarshal(b, &obj2)
	assert.NoError(t, err)

	assertEqualFieldsRoot(t, obj, obj2)
}

func TestRoot_MarshalBinary(t *testing.T) {
	obj := createMinimalSampleRoot()

	b, err := obj.MarshalBinary()
	assert.NoError(t, err)

	obj2 := NewRoot()
	err = obj2.UnmarshalBinary(b)
	assert.NoError(t, err)

	assertEqualFieldsRoot(t, obj, obj2)
}

func TestRoot_FailingMarshalBinary(t *testing.T) {
	obj := createMinimalSampleRoot()
	var err error

	for i := 0; i < 11; i++ {
		enc := &test.GobEncoder{Count: i}
		err = obj.encodeTo(enc)
		assert.Error(t, err)
	}
	// do it again with aliases
	obj._aliases = make(map[string]any)
	for i := 0; i < 12; i++ {
		enc := &test.GobEncoder{Count: i}
		err = obj.encodeTo(enc)
		assert.Error(t, err)
	}
}

func TestRoot_FailingUnmarshalBinary(t *testing.T) {
	obj := createMinimalSampleRoot()
	b, err := obj.MarshalBinary()
	assert.NoError(t, err)
	obj2 := NewRoot()
	for i := 0; i < 11; i++ {
		buf := bytes.NewReader(b)
		dec := &test.GobDecoder{Decoder: gob.NewDecoder(buf), Count: i}
		err = obj2.decodeFrom(dec)
		assert.Error(t, err)
	}

	// do it again with aliases
	obj = createMinimalSampleRoot()
	obj._aliases = map[string]any{"a": 1}
	b, err = obj.MarshalBinary()
	assert.NoError(t, err)

	obj2 = NewRoot()
	for i := 0; i < 12; i++ {
		buf := bytes.NewReader(b)
		dec := &test.GobDecoder{Decoder: gob.NewDecoder(buf), Count: i}
		err = obj2.decodeFrom(dec)
		assert.Error(t, err)
	}
}
