// Code generated by goradd-orm. DO NOT EDIT.

package goradd_unit

import (
	"bytes"
	"context"
	"encoding/gob"
	"encoding/json"
	"strconv"
	"testing"

	"github.com/goradd/orm/_test/gen/orm/goradd_unit/node"
	"github.com/goradd/orm/pkg/op"
	"github.com/goradd/orm/pkg/test"
	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
)

// createMinimalSampleLeafUnl creates an unsaved minimal version of a LeafUnl object
// for testing.
func createMinimalSampleLeafUnl() *LeafUnl {
	obj := NewLeafUnl()
	updateMinimalSampleLeafUnl(obj)

	return obj
}

// updateMinimalSampleLeafUnl sets the values of a minimal sample to new, random values.
func updateMinimalSampleLeafUnl(obj *LeafUnl) {

	obj.SetName(test.RandomValue[string](100))

}

// createMaximalSampleLeafUnl creates an unsaved version of a LeafUnl object
// for testing that includes references to minimal objects.
func createMaximalSampleLeafUnl(ctx context.Context) *LeafUnl {
	obj := NewLeafUnl()
	updateMaximalSampleLeafUnl(ctx, obj)
	return obj
}

// updateMaximalSampleLeafUnl sets all the maximal sample values to new values.
// This will set new values for references, so save the old values and delete them.
func updateMaximalSampleLeafUnl(ctx context.Context, obj *LeafUnl) {
	updateMinimalSampleLeafUnl(obj)
	obj.SetRootUnl(createMinimalSampleRootUnl())

}

// deleteSampleLeafUnl deletes an object created and saved by one of the sample creator functions.
func deleteSampleLeafUnl(ctx context.Context, obj *LeafUnl) {
	if obj == nil {
		return
	}

	_ = obj.Delete(ctx)
	deleteSampleRootUnl(ctx, obj.RootUnl())
}

// assertEqualFieldsLeafUnl compares two objects and asserts that the basic fields are equal.
func assertEqualFieldsLeafUnl(t *testing.T, obj1, obj2 *LeafUnl) {
	if obj1.IDIsLoaded() && obj2.IDIsLoaded() { // only check loaded values
		assert.EqualValues(t, obj1.ID(), obj2.ID())
	}
	if obj1.NameIsLoaded() && obj2.NameIsLoaded() { // only check loaded values
		assert.EqualValues(t, obj1.Name(), obj2.Name())
	}
	if obj1.GroLockIsLoaded() && obj2.GroLockIsLoaded() { // only check loaded values
		assert.EqualValues(t, obj1.GroLock(), obj2.GroLock())
	}

}

func TestLeafUnl_SetID(t *testing.T) {

	obj := NewLeafUnl()

	assert.True(t, obj.IsNew())
	val := test.RandomNumberString()
	obj.SetID(val)
	assert.Equal(t, val, obj.ID())

	// test default
	obj.SetID("")
	assert.EqualValues(t, "", obj.ID(), "set default")

	// test panic on setting value larger than maximum size allowed
	val = test.RandomValue[string](33)
	assert.Panics(t, func() {
		obj.SetID(val)
	})
}
func TestLeafUnl_SetName(t *testing.T) {

	obj := NewLeafUnl()

	assert.True(t, obj.IsNew())
	val := test.RandomValue[string](100)
	obj.SetName(val)
	assert.Equal(t, val, obj.Name())

	// test default
	obj.SetName("")
	assert.EqualValues(t, "", obj.Name(), "set default")

	// test panic on setting value larger than maximum size allowed
	val = test.RandomValue[string](101)
	assert.Panics(t, func() {
		obj.SetName(val)
	})
}
func TestLeafUnl_SetRootUnlID(t *testing.T) {

	obj := NewLeafUnl()

	assert.True(t, obj.IsNew())
	val := test.RandomValue[string](32)
	obj.SetRootUnlID(val)
	assert.Equal(t, val, obj.RootUnlID())
	assert.False(t, obj.RootUnlIDIsNull())

	// Test NULL
	obj.SetRootUnlIDToNull()
	assert.EqualValues(t, "", obj.RootUnlID())
	assert.True(t, obj.RootUnlIDIsNull())

	// test default
	obj.SetRootUnlID("")
	assert.EqualValues(t, "", obj.RootUnlID(), "set default")

	// test panic on setting value larger than maximum size allowed
	val = test.RandomValue[string](33)
	assert.Panics(t, func() {
		obj.SetRootUnlID(val)
	})
}

func TestLeafUnl_Copy(t *testing.T) {
	obj := createMinimalSampleLeafUnl()

	obj2 := obj.Copy()

	assert.Equal(t, obj.Name(), obj2.Name())
	assert.Equal(t, obj.RootUnlID(), obj2.RootUnlID())

}

func TestLeafUnl_BasicInsert(t *testing.T) {
	obj := createMinimalSampleLeafUnl()
	ctx := context.Background()
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleLeafUnl(ctx, obj)

	// Test retrieval
	obj2, err := LoadLeafUnl(ctx, obj.PrimaryKey())
	require.NotNil(t, obj2)
	assert.NoError(t, err)

	assert.Equal(t, obj2.PrimaryKey(), obj2.OriginalPrimaryKey())

	assert.True(t, obj2.IDIsLoaded())
	assert.Panics(t, func() {
		obj2.SetID(obj2.ID())
	})

	assert.True(t, obj2.NameIsLoaded())
	// test that setting it to the same value will not change the dirty bit
	assert.False(t, obj2.nameIsDirty)
	obj2.SetName(obj2.Name())
	assert.False(t, obj2.nameIsDirty)

	assert.True(t, obj2.GroLockIsLoaded())

}

func TestLeafUnl_InsertPanics(t *testing.T) {
	obj := createMinimalSampleLeafUnl()
	_ = obj
	ctx := context.Background()
	_ = ctx

	obj.rootUnl = nil

	obj.nameIsLoaded = false
	assert.Panics(t, func() { obj.Save(ctx) })
	obj.nameIsLoaded = true

}

func TestLeafUnl_BasicUpdate(t *testing.T) {
	obj := createMinimalSampleLeafUnl()
	ctx := context.Background()
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleLeafUnl(ctx, obj)
	updateMinimalSampleLeafUnl(obj)
	assert.NoError(t, obj.Save(ctx))
	obj2, err := LoadLeafUnl(ctx, obj.PrimaryKey())
	assert.NoError(t, err)

	assert.Equal(t, obj2.ID(), obj.ID(), "ID did not update")
	assert.Equal(t, obj2.Name(), obj.Name(), "Name did not update")
	assert.Equal(t, obj2.GroLock(), obj.GroLock(), "GroLock did not update")
}

func TestLeafUnl_ReferenceLoad(t *testing.T) {
	ctx := context.Background()
	obj := createMaximalSampleLeafUnl(ctx)
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleLeafUnl(ctx, obj)

	// Test that referenced objects were saved and assigned ids
	assert.NotNil(t, obj.RootUnl())
	assert.NotEqual(t, '-', obj.RootUnl().PrimaryKey()[0])

	// Test lazy loading
	obj2, err := LoadLeafUnl(ctx, obj.PrimaryKey())
	assert.NoError(t, err)
	assert.NotNil(t, obj2)
	objPkOnly, err2 := LoadLeafUnl(ctx, obj.PrimaryKey(),
		node.LeafUnl().ID())
	assert.NoError(t, err2)
	_ = objPkOnly

	assert.Nil(t, obj2.RootUnl(), "RootUnl is not loaded initially")
	v_RootUnl, _ := obj2.LoadRootUnl(ctx)
	assert.NotNil(t, v_RootUnl)
	assert.Equal(t, v_RootUnl.PrimaryKey(), obj2.RootUnl().PrimaryKey())
	assert.Equal(t, obj.RootUnl().PrimaryKey(), obj2.RootUnl().PrimaryKey())
	assert.True(t, obj2.RootUnlIDIsLoaded())

	assert.False(t, objPkOnly.RootUnlIDIsLoaded())
	assert.Panics(t, func() { _, _ = objPkOnly.LoadRootUnl(ctx) })

	// test eager loading
	obj3, _ := LoadLeafUnl(ctx, obj.PrimaryKey(), node.LeafUnl().RootUnl())
	_ = obj3 // avoid error if there are no references

	assert.Equal(t, obj2.RootUnl().PrimaryKey(), obj3.RootUnl().PrimaryKey())

}

func TestLeafUnl_ReferenceUpdateNewObjects(t *testing.T) {
	ctx := context.Background()
	obj := createMaximalSampleLeafUnl(ctx)
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleLeafUnl(ctx, obj)

	obj2, _ := LoadLeafUnl(ctx, obj.PrimaryKey())
	updateMaximalSampleLeafUnl(ctx, obj2)
	assert.NoError(t, obj2.Save(ctx))
	defer deleteSampleLeafUnl(ctx, obj2)

	obj3, _ := LoadLeafUnl(ctx, obj2.PrimaryKey(), node.LeafUnl().RootUnl())
	_ = obj3 // avoid error if there are no references

	assert.Equal(t, obj2.RootUnl().PrimaryKey(), obj3.RootUnl().PrimaryKey())

}

func TestLeafUnl_ReferenceUpdateOldObjects(t *testing.T) {
	ctx := context.Background()
	obj := createMaximalSampleLeafUnl(ctx)
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleLeafUnl(ctx, obj)

	updateMinimalSampleRootUnl(obj.RootUnl())

	assert.NoError(t, obj.Save(ctx))

	obj2, _ := LoadLeafUnl(ctx, obj.PrimaryKey(), node.LeafUnl().RootUnl())
	_ = obj2 // avoid error if there are no references

	assertEqualFieldsRootUnl(t, obj2.RootUnl(), obj.RootUnl())

}
func TestLeafUnl_EmptyPrimaryKeyGetter(t *testing.T) {
	obj := NewLeafUnl()

	i, err := strconv.Atoi(obj.ID())
	assert.NoError(t, err)
	assert.True(t, i < 0)
}

func TestLeafUnl_Getters(t *testing.T) {
	obj := createMinimalSampleLeafUnl()

	i, err := strconv.Atoi(obj.ID())
	assert.NoError(t, err)
	assert.True(t, i < 0)

	ctx := context.Background()
	require.NoError(t, obj.Save(ctx))
	defer deleteSampleLeafUnl(ctx, obj)

	has, _ := HasLeafUnl(ctx, obj.PrimaryKey())
	assert.True(t, has)

	obj2, _ := LoadLeafUnl(ctx, obj.PrimaryKey(),
		node.LeafUnl().ID())

	assert.Equal(t, obj.ID(), obj.Get(LeafUnlIDField))
	assert.Equal(t, obj.Name(), obj.Get(LeafUnlNameField))
	assert.Panics(t, func() { obj2.Name() })
	assert.Nil(t, obj2.Get(LeafUnlNameField))
	assert.Equal(t, obj.GroLock(), obj.Get(LeafUnlGroLockField))
	assert.Panics(t, func() { obj2.GroLock() })
	assert.Nil(t, obj2.Get(LeafUnlGroLockField))
	// Not loaded
	assert.Nil(t, obj2.RootUnl())
	assert.Nil(t, obj2.Get(LeafUnlRootUnlField))
	assert.Panics(t, func() { obj2.RootUnlID() })
	assert.Nil(t, obj2.Get(LeafUnlRootUnlIDField))

}

func TestLeafUnl_QueryLoad(t *testing.T) {
	obj := createMinimalSampleLeafUnl()
	ctx := context.Background()
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleLeafUnl(ctx, obj)

	objs, err := QueryLeafUnls(ctx).
		Where(op.Equal(node.LeafUnl().ID(), obj.ID())).
		OrderBy(node.LeafUnl().ID()). // exercise order by
		Limit(1, 0).                  // exercise limit
		Calculation(node.LeafUnl(), "IsTrue", op.Equal("A", "A")).
		Load()
	assert.NoError(t, err)
	assert.Equal(t, obj.PrimaryKey(), objs[0].PrimaryKey())
	assert.True(t, objs[0].GetAlias("IsTrue").Bool())
}
func TestLeafUnl_QueryLoadI(t *testing.T) {
	obj := createMinimalSampleLeafUnl()
	ctx := context.Background()
	err := obj.Save(ctx)
	assert.NoError(t, err)
	defer deleteSampleLeafUnl(ctx, obj)

	objs, _ := QueryLeafUnls(ctx).
		Where(op.Equal(node.LeafUnl().ID(), obj.ID())).
		LoadI()

	assert.Equal(t, obj.PrimaryKey(), objs[0].(*LeafUnl).PrimaryKey())
}
func TestLeafUnl_QueryCursor(t *testing.T) {
	obj := createMinimalSampleLeafUnl()
	ctx := context.Background()
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleLeafUnl(ctx, obj)

	cursor, err := QueryLeafUnls(ctx).
		Where(op.Equal(node.LeafUnl().ID(), obj.ID())).
		LoadCursor()
	require.NoError(t, err)
	obj2, err2 := cursor.Next()
	assert.Equal(t, obj.PrimaryKey(), obj2.PrimaryKey())
	require.NoError(t, err2)
	obj2, err2 = cursor.Next()
	assert.Nil(t, obj2)
	require.NoError(t, err2)
	assert.NoError(t, cursor.Close())

	// test empty cursor result
	cursor, err = QueryLeafUnls(ctx).
		Where(op.Equal("B", "A")).
		LoadCursor()
	require.NoError(t, err)

	obj2, err = cursor.Next()
	assert.Nil(t, obj2)
	require.NoError(t, err)
	assert.NoError(t, cursor.Close())
}
func TestLeafUnl_Count(t *testing.T) {
	ctx := context.Background()
	obj := createMaximalSampleLeafUnl(ctx)
	err := obj.Save(ctx)
	assert.NoError(t, err)
	defer deleteSampleLeafUnl(ctx, obj)
	assert.Positive(t, func() int { i, _ := CountLeafUnls(ctx); return i }())

	// reread in case there are data limitations imposed by the database
	obj2, _ := LoadLeafUnl(ctx, obj.PrimaryKey())
	assert.Positive(t,
		func() int {
			i, _ := CountLeafUnlsByRootUnlID(ctx,
				obj2.RootUnlID())
			return i
		}())

}

func TestLeafUnl_MarshalJSON(t *testing.T) {
	obj := createMinimalSampleLeafUnl()

	b, err := json.Marshal(obj)
	assert.NoError(t, err)

	obj2 := NewLeafUnl()
	err = json.Unmarshal(b, &obj2)
	assert.NoError(t, err)

	assertEqualFieldsLeafUnl(t, obj, obj2)
}

func TestLeafUnl_MarshalBinary(t *testing.T) {
	obj := createMinimalSampleLeafUnl()

	b, err := obj.MarshalBinary()
	assert.NoError(t, err)

	obj2 := NewLeafUnl()
	err = obj2.UnmarshalBinary(b)
	assert.NoError(t, err)

	assertEqualFieldsLeafUnl(t, obj, obj2)
}

func TestLeafUnl_FailingMarshalBinary(t *testing.T) {
	obj := createMinimalSampleLeafUnl()
	var err error

	for i := 0; i < 11; i++ {
		enc := &test.GobEncoder{Count: i}
		err = obj.encodeTo(enc)
		assert.Error(t, err)
	}
	// do it again with aliases
	obj._aliases = make(map[string]any)
	for i := 0; i < 12; i++ {
		enc := &test.GobEncoder{Count: i}
		err = obj.encodeTo(enc)
		assert.Error(t, err)
	}
}

func TestLeafUnl_FailingUnmarshalBinary(t *testing.T) {
	obj := createMinimalSampleLeafUnl()
	b, err := obj.MarshalBinary()
	assert.NoError(t, err)
	obj2 := NewLeafUnl()
	for i := 0; i < 11; i++ {
		buf := bytes.NewReader(b)
		dec := &test.GobDecoder{Decoder: gob.NewDecoder(buf), Count: i}
		err = obj2.decodeFrom(dec)
		assert.Error(t, err)
	}

	// do it again with aliases
	obj = createMinimalSampleLeafUnl()
	obj._aliases = map[string]any{"a": 1}
	b, err = obj.MarshalBinary()
	assert.NoError(t, err)

	obj2 = NewLeafUnl()
	for i := 0; i < 12; i++ {
		buf := bytes.NewReader(b)
		dec := &test.GobDecoder{Decoder: gob.NewDecoder(buf), Count: i}
		err = obj2.decodeFrom(dec)
		assert.Error(t, err)
	}
}

func TestLeafUnl_Indexes(t *testing.T) {
	ctx := context.Background()
	obj := createMaximalSampleLeafUnl(ctx)
	err := obj.Save(ctx)
	assert.NoError(t, err)
	defer deleteSampleLeafUnl(ctx, obj)

	var obj2 *LeafUnl
	obj2, _ = LoadLeafUnlByRootUnlID(ctx, obj.RootUnlID())
	assert.Equal(t, obj.PrimaryKey(), obj2.PrimaryKey())
	assert.True(t, func() bool { h, _ := HasLeafUnlByRootUnlID(ctx, obj.RootUnlID()); return h }())

}
