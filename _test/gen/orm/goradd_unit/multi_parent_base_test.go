// Code generated by goradd-orm. DO NOT EDIT.

package goradd_unit

import (
	"bytes"
	"context"
	"encoding/gob"
	"encoding/json"
	"strconv"
	"testing"

	"github.com/goradd/orm/_test/gen/orm/goradd_unit/node"
	"github.com/goradd/orm/pkg/db"
	"github.com/goradd/orm/pkg/op"
	"github.com/goradd/orm/pkg/test"
	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
)

// createMinimalSampleMultiParent creates an unsaved minimal version of a MultiParent object
// for testing.
func createMinimalSampleMultiParent() *MultiParent {
	obj := NewMultiParent()
	updateMinimalSampleMultiParent(obj)

	return obj
}

// updateMinimalSampleMultiParent sets the values of a minimal sample to new, random values.
func updateMinimalSampleMultiParent(obj *MultiParent) {

	obj.SetName(test.RandomValue[string](100))

}

// createMaximalSampleMultiParent creates an unsaved version of a MultiParent object
// for testing that includes references to minimal objects.
func createMaximalSampleMultiParent(ctx context.Context) *MultiParent {
	obj := NewMultiParent()
	updateMaximalSampleMultiParent(ctx, obj)
	return obj
}

// updateMaximalSampleMultiParent sets all the maximal sample values to new values.
// This will set new values for references, so save the old values and delete them.
func updateMaximalSampleMultiParent(ctx context.Context, obj *MultiParent) {
	updateMinimalSampleMultiParent(obj)
	obj.SetParent1(createMinimalSampleMultiParent())
	obj.SetParent2(createMinimalSampleMultiParent())

	obj.SetParent1MultiParents(createMinimalSampleMultiParent())
	obj.SetParent2MultiParents(createMinimalSampleMultiParent())
}

// deleteSampleMultiParent deletes an object created and saved by one of the sample creator functions.
func deleteSampleMultiParent(ctx context.Context, obj *MultiParent) {
	if obj == nil {
		return
	}

	for _, item := range obj.Parent1MultiParents() {
		deleteSampleMultiParent(ctx, item)
	}
	for _, item := range obj.Parent2MultiParents() {
		deleteSampleMultiParent(ctx, item)
	}

	_ = obj.Delete(ctx)

	deleteSampleMultiParent(ctx, obj.Parent1())

	deleteSampleMultiParent(ctx, obj.Parent2())

}

// assertEqualFieldsMultiParent compares two objects and asserts that the basic fields are equal.
func assertEqualFieldsMultiParent(t *testing.T, obj1, obj2 *MultiParent) {
	if obj1.IDIsLoaded() && obj2.IDIsLoaded() { // only check loaded values
		assert.EqualValues(t, obj1.ID(), obj2.ID())
	}
	if obj1.NameIsLoaded() && obj2.NameIsLoaded() { // only check loaded values
		assert.EqualValues(t, obj1.Name(), obj2.Name())
	}
	if obj1.Parent1IDIsLoaded() && obj2.Parent1IDIsLoaded() { // only check loaded values
		assert.EqualValues(t, obj1.Parent1ID(), obj2.Parent1ID())
	}
	if obj1.Parent2IDIsLoaded() && obj2.Parent2IDIsLoaded() { // only check loaded values
		assert.EqualValues(t, obj1.Parent2ID(), obj2.Parent2ID())
	}

}

func TestMultiParent_SetID(t *testing.T) {

	obj := NewMultiParent()

	assert.True(t, obj.IsNew())
	val := test.RandomNumberString()
	obj.SetID(val)
	assert.Equal(t, val, obj.ID())

	// test default
	obj.SetID("")
	assert.EqualValues(t, "", obj.ID(), "set default")

	// test panic on setting value larger than maximum size allowed
	val = test.RandomValue[string](33)
	assert.Panics(t, func() {
		obj.SetID(val)
	})
}
func TestMultiParent_SetName(t *testing.T) {

	obj := NewMultiParent()

	assert.True(t, obj.IsNew())
	val := test.RandomValue[string](100)
	obj.SetName(val)
	assert.Equal(t, val, obj.Name())
	assert.False(t, obj.NameIsNull())

	// Test NULL
	obj.SetNameToNull()
	assert.EqualValues(t, "", obj.Name())
	assert.True(t, obj.NameIsNull())

	// test default
	obj.SetName("")
	assert.EqualValues(t, "", obj.Name(), "set default")

	// test panic on setting value larger than maximum size allowed
	val = test.RandomValue[string](101)
	assert.Panics(t, func() {
		obj.SetName(val)
	})
}
func TestMultiParent_SetParent1ID(t *testing.T) {

	obj := NewMultiParent()

	assert.True(t, obj.IsNew())
	val := test.RandomValue[string](0)
	obj.SetParent1ID(val)
	assert.Equal(t, val, obj.Parent1ID())
	assert.False(t, obj.Parent1IDIsNull())

	// Test NULL
	obj.SetParent1IDToNull()
	assert.EqualValues(t, "", obj.Parent1ID())
	assert.True(t, obj.Parent1IDIsNull())

	// test default
	obj.SetParent1ID("")
	assert.EqualValues(t, "", obj.Parent1ID(), "set default")

}
func TestMultiParent_SetParent2ID(t *testing.T) {

	obj := NewMultiParent()

	assert.True(t, obj.IsNew())
	val := test.RandomValue[string](0)
	obj.SetParent2ID(val)
	assert.Equal(t, val, obj.Parent2ID())
	assert.False(t, obj.Parent2IDIsNull())

	// Test NULL
	obj.SetParent2IDToNull()
	assert.EqualValues(t, "", obj.Parent2ID())
	assert.True(t, obj.Parent2IDIsNull())

	// test default
	obj.SetParent2ID("")
	assert.EqualValues(t, "", obj.Parent2ID(), "set default")

}

func TestMultiParent_Copy(t *testing.T) {
	obj := createMinimalSampleMultiParent()

	obj2 := obj.Copy()

	assert.Equal(t, obj.Name(), obj2.Name())
	assert.Equal(t, obj.Parent1ID(), obj2.Parent1ID())
	assert.Equal(t, obj.Parent2ID(), obj2.Parent2ID())

}

func TestMultiParent_BasicInsert(t *testing.T) {
	obj := createMinimalSampleMultiParent()
	ctx := db.NewContext(nil)
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleMultiParent(ctx, obj)

	// Test retrieval
	obj2, err := LoadMultiParent(ctx, obj.PrimaryKey())
	require.NotNil(t, obj2)
	assert.NoError(t, err)

	assert.Equal(t, obj2.PrimaryKey(), obj2.OriginalPrimaryKey())

	assert.True(t, obj2.IDIsLoaded())
	assert.Panics(t, func() {
		obj2.SetID(obj2.ID())
	})

	assert.True(t, obj2.NameIsLoaded())
	assert.False(t, obj2.NameIsNull())
	// test that setting it to the same value will not change the dirty bit
	assert.False(t, obj2.nameIsDirty)
	obj2.SetName(obj2.Name())
	assert.False(t, obj2.nameIsDirty)

}

func TestMultiParent_InsertPanics(t *testing.T) {
	obj := createMinimalSampleMultiParent()
	_ = obj
	ctx := db.NewContext(nil)
	_ = ctx

}

func TestMultiParent_BasicUpdate(t *testing.T) {
	obj := createMinimalSampleMultiParent()
	ctx := db.NewContext(nil)
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleMultiParent(ctx, obj)
	updateMinimalSampleMultiParent(obj)
	assert.NoError(t, obj.Save(ctx))
	obj2, err := LoadMultiParent(ctx, obj.PrimaryKey())
	assert.NoError(t, err)

	assert.Equal(t, obj2.ID(), obj.ID(), "ID did not update")
	assert.Equal(t, obj2.Name(), obj.Name(), "Name did not update")
}

func TestMultiParent_ReferenceLoad(t *testing.T) {
	ctx := db.NewContext(nil)
	obj := createMaximalSampleMultiParent(ctx)
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleMultiParent(ctx, obj)

	// Test that referenced objects were saved and assigned ids
	assert.NotNil(t, obj.Parent1())
	assert.NotEqual(t, '-', obj.Parent1().PrimaryKey()[0])

	assert.NotNil(t, obj.Parent2())
	assert.NotEqual(t, '-', obj.Parent2().PrimaryKey()[0])

	// Test lazy loading
	obj2, err := LoadMultiParent(ctx, obj.PrimaryKey())
	assert.NoError(t, err)
	objPkOnly, err2 := LoadMultiParent(ctx, obj.PrimaryKey(), node.MultiParent().PrimaryKey())
	assert.NoError(t, err2)
	_ = obj2 // avoid error if there are no references
	_ = objPkOnly

	assert.Nil(t, obj2.Parent1(), "Parent1 is not loaded initially")
	v_Parent1, _ := obj2.LoadParent1(ctx)
	assert.NotNil(t, v_Parent1)
	assert.Equal(t, v_Parent1.PrimaryKey(), obj2.Parent1().PrimaryKey())
	assert.Equal(t, obj.Parent1().PrimaryKey(), obj2.Parent1().PrimaryKey())
	assert.True(t, obj2.Parent1IDIsLoaded())

	assert.False(t, objPkOnly.Parent1IDIsLoaded())
	assert.Panics(t, func() { _, _ = objPkOnly.LoadParent1(ctx) })

	assert.Nil(t, obj2.Parent2(), "Parent2 is not loaded initially")
	v_Parent2, _ := obj2.LoadParent2(ctx)
	assert.NotNil(t, v_Parent2)
	assert.Equal(t, v_Parent2.PrimaryKey(), obj2.Parent2().PrimaryKey())
	assert.Equal(t, obj.Parent2().PrimaryKey(), obj2.Parent2().PrimaryKey())
	assert.True(t, obj2.Parent2IDIsLoaded())

	assert.False(t, objPkOnly.Parent2IDIsLoaded())
	assert.Panics(t, func() { _, _ = objPkOnly.LoadParent2(ctx) })

	assert.Nil(t, obj2.Parent1MultiParents(), "Parent1MultiParents is not loaded initially")
	v_Parent1MultiParents, _ := obj2.LoadParent1MultiParents(ctx)
	assert.NotNil(t, v_Parent1MultiParents)
	assert.Len(t, v_Parent1MultiParents, 1)
	assert.Nil(t, obj2.Parent2MultiParents(), "Parent2MultiParents is not loaded initially")
	v_Parent2MultiParents, _ := obj2.LoadParent2MultiParents(ctx)
	assert.NotNil(t, v_Parent2MultiParents)
	assert.Len(t, v_Parent2MultiParents, 1)

	// test eager loading
	obj3, _ := LoadMultiParent(ctx, obj.PrimaryKey(), node.MultiParent().Parent1(),
		node.MultiParent().Parent2(),
		node.MultiParent().Parent1MultiParents(),
		node.MultiParent().Parent2MultiParents(),
	)
	_ = obj3 // avoid error if there are no references

	assert.Equal(t, obj2.Parent1().PrimaryKey(), obj3.Parent1().PrimaryKey())
	assert.Equal(t, obj2.Parent2().PrimaryKey(), obj3.Parent2().PrimaryKey())
	assert.Equal(t, len(obj2.Parent1MultiParents()), len(obj3.Parent1MultiParents()))
	assert.Equal(t, len(obj2.Parent2MultiParents()), len(obj3.Parent2MultiParents()))

}

func TestMultiParent_ReferenceUpdateNewObjects(t *testing.T) {
	ctx := db.NewContext(nil)
	obj := createMaximalSampleMultiParent(ctx)
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleMultiParent(ctx, obj)

	obj2, _ := LoadMultiParent(ctx, obj.PrimaryKey())
	updateMaximalSampleMultiParent(ctx, obj2)
	assert.NoError(t, obj2.Save(ctx))
	defer deleteSampleMultiParent(ctx, obj2)

	obj3, _ := LoadMultiParent(ctx, obj2.PrimaryKey(), node.MultiParent().Parent1(),
		node.MultiParent().Parent2(),
		node.MultiParent().Parent1MultiParents(),
		node.MultiParent().Parent2MultiParents(),
	)
	_ = obj3 // avoid error if there are no references

	assert.Equal(t, obj2.Parent1().PrimaryKey(), obj3.Parent1().PrimaryKey())
	assert.Equal(t, obj2.Parent2().PrimaryKey(), obj3.Parent2().PrimaryKey())

	assert.Equal(t, len(obj2.Parent1MultiParents()), len(obj3.Parent1MultiParents()))
	assert.Equal(t, len(obj2.Parent2MultiParents()), len(obj3.Parent2MultiParents()))

}

func TestMultiParent_ReferenceUpdateOldObjects(t *testing.T) {
	ctx := db.NewContext(nil)
	obj := createMaximalSampleMultiParent(ctx)
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleMultiParent(ctx, obj)

	updateMinimalSampleMultiParent(obj.Parent1())
	updateMinimalSampleMultiParent(obj.Parent2())
	updateMinimalSampleMultiParent(obj.Parent1MultiParents()[0])
	updateMinimalSampleMultiParent(obj.Parent2MultiParents()[0])

	assert.NoError(t, obj.Save(ctx))

	obj2, _ := LoadMultiParent(ctx, obj.PrimaryKey(),

		node.MultiParent().Parent1(),

		node.MultiParent().Parent2(),

		node.MultiParent().Parent1MultiParents(),
		node.MultiParent().Parent2MultiParents(),
	)
	_ = obj2 // avoid error if there are no references

	assertEqualFieldsMultiParent(t, obj2.Parent1(), obj.Parent1())
	assertEqualFieldsMultiParent(t, obj2.Parent2(), obj.Parent2())

	assertEqualFieldsMultiParent(t, obj2.Parent1MultiParents()[0], obj.Parent1MultiParents()[0])
	assertEqualFieldsMultiParent(t, obj2.Parent2MultiParents()[0], obj.Parent2MultiParents()[0])

}
func TestMultiParent_EmptyPrimaryKeyGetter(t *testing.T) {
	obj := NewMultiParent()

	i, err := strconv.Atoi(obj.ID())
	assert.NoError(t, err)
	assert.True(t, i < 0)
}

func TestMultiParent_Getters(t *testing.T) {
	obj := createMinimalSampleMultiParent()

	i, err := strconv.Atoi(obj.ID())
	assert.NoError(t, err)
	assert.True(t, i < 0)

	ctx := db.NewContext(nil)
	require.NoError(t, obj.Save(ctx))
	defer deleteSampleMultiParent(ctx, obj)

	has, _ := HasMultiParent(ctx, obj.PrimaryKey())
	assert.True(t, has)

	obj2, _ := LoadMultiParent(ctx, obj.PrimaryKey(), node.MultiParent().PrimaryKey())

	assert.Equal(t, obj.ID(), obj.Get(node.MultiParent().ID().Identifier))
	assert.Equal(t, obj.Name(), obj.Get(node.MultiParent().Name().Identifier))
	assert.Panics(t, func() { obj2.Name() })
	assert.Nil(t, obj2.Get(node.MultiParent().Name().Identifier))
	assert.Panics(t, func() { obj2.Parent1ID() })
	assert.Nil(t, obj2.Get(node.MultiParent().Parent1ID().Identifier))
	assert.Panics(t, func() { obj2.Parent2ID() })
	assert.Nil(t, obj2.Get(node.MultiParent().Parent2ID().Identifier))
}

func TestMultiParent_QueryLoad(t *testing.T) {
	obj := createMinimalSampleMultiParent()
	ctx := db.NewContext(nil)
	err := obj.Save(ctx)
	assert.NoError(t, err)
	defer deleteSampleMultiParent(ctx, obj)

	objs, _ := QueryMultiParents(ctx).
		Where(op.Equal(node.MultiParent().PrimaryKey(), obj.PrimaryKey())).
		OrderBy(node.MultiParent().PrimaryKey()). // exercise order by
		Limit(1, 0).                              // exercise limit
		Calculation(node.MultiParent(), "IsTrue", op.Equal(1, 1)).
		Load()

	assert.Equal(t, obj.PrimaryKey(), objs[0].PrimaryKey())
	assert.True(t, objs[0].GetAlias("IsTrue").Bool())
}
func TestMultiParent_QueryLoadI(t *testing.T) {
	obj := createMinimalSampleMultiParent()
	ctx := db.NewContext(nil)
	err := obj.Save(ctx)
	assert.NoError(t, err)
	defer deleteSampleMultiParent(ctx, obj)

	objs, _ := QueryMultiParents(ctx).
		Where(op.Equal(node.MultiParent().PrimaryKey(), obj.PrimaryKey())).
		LoadI()

	assert.Equal(t, obj.PrimaryKey(), objs[0].Get("ID"))
}
func TestMultiParent_QueryCursor(t *testing.T) {
	obj := createMinimalSampleMultiParent()
	ctx := db.NewContext(nil)
	err := obj.Save(ctx)
	assert.NoError(t, err)
	defer deleteSampleMultiParent(ctx, obj)

	cursor, _ := QueryMultiParents(ctx).
		Where(op.Equal(node.MultiParent().PrimaryKey(), obj.PrimaryKey())).
		LoadCursor()

	obj2, err2 := cursor.Next()
	assert.Equal(t, obj.PrimaryKey(), obj2.PrimaryKey())
	assert.NoError(t, err2)
	obj2, err2 = cursor.Next()
	assert.Nil(t, obj2)
	assert.NoError(t, err2)
	assert.NoError(t, cursor.Close())

	// test empty cursor result
	cursor, err = QueryMultiParents(ctx).
		Where(op.Equal(1, 0)).
		LoadCursor()
	obj2, err = cursor.Next()
	assert.Nil(t, obj2)
	assert.NoError(t, err)
	assert.NoError(t, cursor.Close())
}
func TestMultiParent_Count(t *testing.T) {
	ctx := db.NewContext(nil)
	obj := createMaximalSampleMultiParent(ctx)
	err := obj.Save(ctx)
	assert.NoError(t, err)
	defer deleteSampleMultiParent(ctx, obj)
	// reread in case there are data limitations imposed by the database
	obj2, _ := LoadMultiParent(ctx, obj.PrimaryKey())

	assert.Positive(t, func() int { i, _ := CountMultiParents(ctx); return i }())
	assert.Positive(t, func() int { i, _ := CountMultiParentsByID(ctx, obj2.ID()); return i }())
	assert.Positive(t, func() int { i, _ := CountMultiParentsByName(ctx, obj2.Name()); return i }())
	assert.Positive(t, func() int { i, _ := CountMultiParentsByParent1ID(ctx, obj2.Parent1ID()); return i }())
	assert.Positive(t, func() int { i, _ := CountMultiParentsByParent2ID(ctx, obj2.Parent2ID()); return i }())

}

func TestMultiParent_MarshalJSON(t *testing.T) {
	obj := createMinimalSampleMultiParent()

	b, err := json.Marshal(obj)
	assert.NoError(t, err)

	obj2 := NewMultiParent()
	err = json.Unmarshal(b, &obj2)
	assert.NoError(t, err)

	assertEqualFieldsMultiParent(t, obj, obj2)
}

func TestMultiParent_MarshalBinary(t *testing.T) {
	obj := createMinimalSampleMultiParent()

	b, err := obj.MarshalBinary()
	assert.NoError(t, err)

	obj2 := NewMultiParent()
	err = obj2.UnmarshalBinary(b)
	assert.NoError(t, err)

	assertEqualFieldsMultiParent(t, obj, obj2)
}

func TestMultiParent_FailingMarshalBinary(t *testing.T) {
	obj := createMinimalSampleMultiParent()
	var err error

	for i := 0; i < 22; i++ {
		enc := &test.GobEncoder{Count: i}
		err = obj.encodeTo(enc)
		assert.Error(t, err)
	}
	// do it again with aliases
	obj._aliases = make(map[string]any)
	for i := 0; i < 23; i++ {
		enc := &test.GobEncoder{Count: i}
		err = obj.encodeTo(enc)
		assert.Error(t, err)
	}
}

func TestMultiParent_FailingUnmarshalBinary(t *testing.T) {
	obj := createMinimalSampleMultiParent()
	b, err := obj.MarshalBinary()
	assert.NoError(t, err)
	obj2 := NewMultiParent()
	for i := 0; i < 22; i++ {
		buf := bytes.NewReader(b)
		dec := &test.GobDecoder{Decoder: gob.NewDecoder(buf), Count: i}
		err = obj2.decodeFrom(dec)
		assert.Error(t, err)
	}

	// do it again with aliases
	obj = createMinimalSampleMultiParent()
	obj._aliases = map[string]any{"a": 1}
	b, err = obj.MarshalBinary()
	assert.NoError(t, err)

	obj2 = NewMultiParent()
	for i := 0; i < 23; i++ {
		buf := bytes.NewReader(b)
		dec := &test.GobDecoder{Decoder: gob.NewDecoder(buf), Count: i}
		err = obj2.decodeFrom(dec)
		assert.Error(t, err)
	}
}
