// Code generated by goradd-orm. DO NOT EDIT.

package goradd_unit

import (
	"bytes"
	"context"
	"encoding/gob"
	"encoding/json"
	"strconv"
	"testing"

	"github.com/goradd/orm/_test/gen/orm/goradd_unit/node"
	"github.com/goradd/orm/pkg/db"
	"github.com/goradd/orm/pkg/op"
	"github.com/goradd/orm/pkg/test"
	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
)

// createMinimalSampleAltLeafUn creates an unsaved minimal version of a AltLeafUn object
// for testing.
func createMinimalSampleAltLeafUn() *AltLeafUn {
	obj := NewAltLeafUn()
	updateMinimalSampleAltLeafUn(obj)

	return obj
}

// updateMinimalSampleAltLeafUn sets the values of a minimal sample to new, random values.
func updateMinimalSampleAltLeafUn(obj *AltLeafUn) {

	obj.SetName(test.RandomValue[string](100))

}

// createMaximalSampleAltLeafUn creates an unsaved version of a AltLeafUn object
// for testing that includes references to minimal objects.
func createMaximalSampleAltLeafUn(ctx context.Context) *AltLeafUn {
	obj := NewAltLeafUn()
	updateMaximalSampleAltLeafUn(ctx, obj)
	return obj
}

// updateMaximalSampleAltLeafUn sets all the maximal sample values to new values.
// This will set new values for references, so save the old values and delete them.
func updateMaximalSampleAltLeafUn(ctx context.Context, obj *AltLeafUn) {
	updateMinimalSampleAltLeafUn(obj)
	obj.SetAltRootUn(createMinimalSampleAltRootUn())

}

// deleteSampleAltLeafUn deletes an object created and saved by one of the sample creator functions.
func deleteSampleAltLeafUn(ctx context.Context, obj *AltLeafUn) {
	if obj == nil {
		return
	}

	_ = obj.Delete(ctx)

	deleteSampleAltRootUn(ctx, obj.AltRootUn())

}

// assertEqualFieldsAltLeafUn compares two objects and asserts that the basic fields are equal.
func assertEqualFieldsAltLeafUn(t *testing.T, obj1, obj2 *AltLeafUn) {
	if obj1.IDIsLoaded() && obj2.IDIsLoaded() { // only check loaded values
		assert.EqualValues(t, obj1.ID(), obj2.ID())
	}
	if obj1.NameIsLoaded() && obj2.NameIsLoaded() { // only check loaded values
		assert.EqualValues(t, obj1.Name(), obj2.Name())
	}
	if obj1.AltRootUnIDIsLoaded() && obj2.AltRootUnIDIsLoaded() { // only check loaded values
		assert.EqualValues(t, obj1.AltRootUnID(), obj2.AltRootUnID())
	}

}

func TestAltLeafUn_SetID(t *testing.T) {

	obj := NewAltLeafUn()

	assert.True(t, obj.IsNew())
	val := test.RandomNumberString()
	obj.SetID(val)
	assert.Equal(t, val, obj.ID())

	// test default
	obj.SetID("")
	assert.EqualValues(t, "", obj.ID(), "set default")

	// test panic on setting value larger than maximum size allowed
	val = test.RandomValue[string](33)
	assert.Panics(t, func() {
		obj.SetID(val)
	})
}
func TestAltLeafUn_SetName(t *testing.T) {

	obj := NewAltLeafUn()

	assert.True(t, obj.IsNew())
	val := test.RandomValue[string](100)
	obj.SetName(val)
	assert.Equal(t, val, obj.Name())

	// test default
	obj.SetName("")
	assert.EqualValues(t, "", obj.Name(), "set default")

	// test panic on setting value larger than maximum size allowed
	val = test.RandomValue[string](101)
	assert.Panics(t, func() {
		obj.SetName(val)
	})
}
func TestAltLeafUn_SetAltRootUnID(t *testing.T) {

	obj := NewAltLeafUn()

	assert.True(t, obj.IsNew())
	val := test.RandomValue[float32](0)
	obj.SetAltRootUnID(val)
	assert.Equal(t, val, obj.AltRootUnID())
	assert.False(t, obj.AltRootUnIDIsNull())

	// Test NULL
	obj.SetAltRootUnIDToNull()
	assert.EqualValues(t, 0, obj.AltRootUnID())
	assert.True(t, obj.AltRootUnIDIsNull())

	// test default
	obj.SetAltRootUnID(0)
	assert.EqualValues(t, 0, obj.AltRootUnID(), "set default")

}

func TestAltLeafUn_Copy(t *testing.T) {
	obj := createMinimalSampleAltLeafUn()

	obj2 := obj.Copy()

	assert.Equal(t, obj.Name(), obj2.Name())
	assert.Equal(t, obj.AltRootUnID(), obj2.AltRootUnID())

}

func TestAltLeafUn_BasicInsert(t *testing.T) {
	obj := createMinimalSampleAltLeafUn()
	ctx := db.NewContext(nil)
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleAltLeafUn(ctx, obj)

	// Test retrieval
	obj2, err := LoadAltLeafUn(ctx, obj.PrimaryKey())
	require.NotNil(t, obj2)
	assert.NoError(t, err)

	assert.Equal(t, obj2.PrimaryKey(), obj2.OriginalPrimaryKey())

	assert.True(t, obj2.IDIsLoaded())
	assert.Panics(t, func() {
		obj2.SetID(obj2.ID())
	})

	assert.True(t, obj2.NameIsLoaded())
	// test that setting it to the same value will not change the dirty bit
	assert.False(t, obj2.nameIsDirty)
	obj2.SetName(obj2.Name())
	assert.False(t, obj2.nameIsDirty)

}

func TestAltLeafUn_InsertPanics(t *testing.T) {
	obj := createMinimalSampleAltLeafUn()
	_ = obj
	ctx := db.NewContext(nil)
	_ = ctx

	obj.nameIsLoaded = false
	assert.Panics(t, func() { obj.Save(ctx) })
	obj.nameIsLoaded = true

}

func TestAltLeafUn_BasicUpdate(t *testing.T) {
	obj := createMinimalSampleAltLeafUn()
	ctx := db.NewContext(nil)
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleAltLeafUn(ctx, obj)
	updateMinimalSampleAltLeafUn(obj)
	assert.NoError(t, obj.Save(ctx))
	obj2, err := LoadAltLeafUn(ctx, obj.PrimaryKey())
	assert.NoError(t, err)

	assert.Equal(t, obj2.ID(), obj.ID(), "ID did not update")
	assert.Equal(t, obj2.Name(), obj.Name(), "Name did not update")
}

func TestAltLeafUn_ReferenceLoad(t *testing.T) {
	ctx := db.NewContext(nil)
	obj := createMaximalSampleAltLeafUn(ctx)
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleAltLeafUn(ctx, obj)

	// Test that referenced objects were saved and assigned ids
	assert.NotNil(t, obj.AltRootUn())

	// Test lazy loading
	obj2, err := LoadAltLeafUn(ctx, obj.PrimaryKey())
	assert.NoError(t, err)
	objPkOnly, err2 := LoadAltLeafUn(ctx, obj.PrimaryKey(), node.AltLeafUn().PrimaryKey())
	assert.NoError(t, err2)
	_ = obj2 // avoid error if there are no references
	_ = objPkOnly

	assert.Nil(t, obj2.AltRootUn(), "AltRootUn is not loaded initially")
	v_AltRootUn, _ := obj2.LoadAltRootUn(ctx)
	assert.NotNil(t, v_AltRootUn)
	assert.Equal(t, v_AltRootUn.PrimaryKey(), obj2.AltRootUn().PrimaryKey())
	assert.Equal(t, obj.AltRootUn().PrimaryKey(), obj2.AltRootUn().PrimaryKey())
	assert.True(t, obj2.AltRootUnIDIsLoaded())

	assert.False(t, objPkOnly.AltRootUnIDIsLoaded())
	assert.Panics(t, func() { _, _ = objPkOnly.LoadAltRootUn(ctx) })

	// test eager loading
	obj3, _ := LoadAltLeafUn(ctx, obj.PrimaryKey(), node.AltLeafUn().AltRootUn())
	_ = obj3 // avoid error if there are no references

	assert.Equal(t, obj2.AltRootUn().PrimaryKey(), obj3.AltRootUn().PrimaryKey())

}

func TestAltLeafUn_ReferenceUpdateNewObjects(t *testing.T) {
	ctx := db.NewContext(nil)
	obj := createMaximalSampleAltLeafUn(ctx)
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleAltLeafUn(ctx, obj)

	obj2, _ := LoadAltLeafUn(ctx, obj.PrimaryKey())
	updateMaximalSampleAltLeafUn(ctx, obj2)
	assert.NoError(t, obj2.Save(ctx))
	defer deleteSampleAltLeafUn(ctx, obj2)

	obj3, _ := LoadAltLeafUn(ctx, obj2.PrimaryKey(), node.AltLeafUn().AltRootUn())
	_ = obj3 // avoid error if there are no references

	assert.Equal(t, obj2.AltRootUn().PrimaryKey(), obj3.AltRootUn().PrimaryKey())

}

func TestAltLeafUn_ReferenceUpdateOldObjects(t *testing.T) {
	ctx := db.NewContext(nil)
	obj := createMaximalSampleAltLeafUn(ctx)
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleAltLeafUn(ctx, obj)

	updateMinimalSampleAltRootUn(obj.AltRootUn())

	assert.NoError(t, obj.Save(ctx))

	obj2, _ := LoadAltLeafUn(ctx, obj.PrimaryKey(),

		node.AltLeafUn().AltRootUn(),
	)
	_ = obj2 // avoid error if there are no references

	assertEqualFieldsAltRootUn(t, obj2.AltRootUn(), obj.AltRootUn())

}
func TestAltLeafUn_EmptyPrimaryKeyGetter(t *testing.T) {
	obj := NewAltLeafUn()

	i, err := strconv.Atoi(obj.ID())
	assert.NoError(t, err)
	assert.True(t, i < 0)
}

func TestAltLeafUn_Getters(t *testing.T) {
	obj := createMinimalSampleAltLeafUn()

	i, err := strconv.Atoi(obj.ID())
	assert.NoError(t, err)
	assert.True(t, i < 0)

	ctx := db.NewContext(nil)
	require.NoError(t, obj.Save(ctx))
	defer deleteSampleAltLeafUn(ctx, obj)

	has, _ := HasAltLeafUn(ctx, obj.PrimaryKey())
	assert.True(t, has)

	obj2, _ := LoadAltLeafUn(ctx, obj.PrimaryKey(), node.AltLeafUn().PrimaryKey())

	assert.Equal(t, obj.ID(), obj.Get(node.AltLeafUn().ID().Identifier))
	assert.Equal(t, obj.Name(), obj.Get(node.AltLeafUn().Name().Identifier))
	assert.Panics(t, func() { obj2.Name() })
	assert.Nil(t, obj2.Get(node.AltLeafUn().Name().Identifier))
	assert.Panics(t, func() { obj2.AltRootUnID() })
	assert.Nil(t, obj2.Get(node.AltLeafUn().AltRootUnID().Identifier))
}

func TestAltLeafUn_QueryLoad(t *testing.T) {
	obj := createMinimalSampleAltLeafUn()
	ctx := db.NewContext(nil)
	err := obj.Save(ctx)
	assert.NoError(t, err)
	defer deleteSampleAltLeafUn(ctx, obj)

	objs, _ := QueryAltLeafUns(ctx).
		Where(op.Equal(node.AltLeafUn().PrimaryKey(), obj.PrimaryKey())).
		OrderBy(node.AltLeafUn().PrimaryKey()). // exercise order by
		Limit(1, 0).                            // exercise limit
		Calculation(node.AltLeafUn(), "IsTrue", op.Equal(1, 1)).
		Load()

	assert.Equal(t, obj.PrimaryKey(), objs[0].PrimaryKey())
	assert.True(t, objs[0].GetAlias("IsTrue").Bool())
}
func TestAltLeafUn_QueryLoadI(t *testing.T) {
	obj := createMinimalSampleAltLeafUn()
	ctx := db.NewContext(nil)
	err := obj.Save(ctx)
	assert.NoError(t, err)
	defer deleteSampleAltLeafUn(ctx, obj)

	objs, _ := QueryAltLeafUns(ctx).
		Where(op.Equal(node.AltLeafUn().PrimaryKey(), obj.PrimaryKey())).
		LoadI()

	assert.Equal(t, obj.PrimaryKey(), objs[0].Get("ID"))
}
func TestAltLeafUn_QueryCursor(t *testing.T) {
	obj := createMinimalSampleAltLeafUn()
	ctx := db.NewContext(nil)
	err := obj.Save(ctx)
	assert.NoError(t, err)
	defer deleteSampleAltLeafUn(ctx, obj)

	cursor, _ := QueryAltLeafUns(ctx).
		Where(op.Equal(node.AltLeafUn().PrimaryKey(), obj.PrimaryKey())).
		LoadCursor()

	obj2, err2 := cursor.Next()
	assert.Equal(t, obj.PrimaryKey(), obj2.PrimaryKey())
	assert.NoError(t, err2)
	obj2, err2 = cursor.Next()
	assert.Nil(t, obj2)
	assert.NoError(t, err2)
	assert.NoError(t, cursor.Close())

	// test empty cursor result
	cursor, err = QueryAltLeafUns(ctx).
		Where(op.Equal(1, 0)).
		LoadCursor()
	obj2, err = cursor.Next()
	assert.Nil(t, obj2)
	assert.NoError(t, err)
	assert.NoError(t, cursor.Close())
}
func TestAltLeafUn_Count(t *testing.T) {
	ctx := db.NewContext(nil)
	obj := createMaximalSampleAltLeafUn(ctx)
	err := obj.Save(ctx)
	assert.NoError(t, err)
	defer deleteSampleAltLeafUn(ctx, obj)
	assert.Positive(t, func() int { i, _ := CountAltLeafUns(ctx); return i }())

	// reread in case there are data limitations imposed by the database
	obj2, _ := LoadAltLeafUn(ctx, obj.PrimaryKey())
	assert.Positive(t,
		func() int {
			i, _ := CountAltLeafUnsByAltRootUnID(ctx,
				obj2.AltRootUnID())
			return i
		}())

}

func TestAltLeafUn_MarshalJSON(t *testing.T) {
	obj := createMinimalSampleAltLeafUn()

	b, err := json.Marshal(obj)
	assert.NoError(t, err)

	obj2 := NewAltLeafUn()
	err = json.Unmarshal(b, &obj2)
	assert.NoError(t, err)

	assertEqualFieldsAltLeafUn(t, obj, obj2)
}

func TestAltLeafUn_MarshalBinary(t *testing.T) {
	obj := createMinimalSampleAltLeafUn()

	b, err := obj.MarshalBinary()
	assert.NoError(t, err)

	obj2 := NewAltLeafUn()
	err = obj2.UnmarshalBinary(b)
	assert.NoError(t, err)

	assertEqualFieldsAltLeafUn(t, obj, obj2)
}

func TestAltLeafUn_FailingMarshalBinary(t *testing.T) {
	obj := createMinimalSampleAltLeafUn()
	var err error

	for i := 0; i < 13; i++ {
		enc := &test.GobEncoder{Count: i}
		err = obj.encodeTo(enc)
		assert.Error(t, err)
	}
	// do it again with aliases
	obj._aliases = make(map[string]any)
	for i := 0; i < 14; i++ {
		enc := &test.GobEncoder{Count: i}
		err = obj.encodeTo(enc)
		assert.Error(t, err)
	}
}

func TestAltLeafUn_FailingUnmarshalBinary(t *testing.T) {
	obj := createMinimalSampleAltLeafUn()
	b, err := obj.MarshalBinary()
	assert.NoError(t, err)
	obj2 := NewAltLeafUn()
	for i := 0; i < 13; i++ {
		buf := bytes.NewReader(b)
		dec := &test.GobDecoder{Decoder: gob.NewDecoder(buf), Count: i}
		err = obj2.decodeFrom(dec)
		assert.Error(t, err)
	}

	// do it again with aliases
	obj = createMinimalSampleAltLeafUn()
	obj._aliases = map[string]any{"a": 1}
	b, err = obj.MarshalBinary()
	assert.NoError(t, err)

	obj2 = NewAltLeafUn()
	for i := 0; i < 14; i++ {
		buf := bytes.NewReader(b)
		dec := &test.GobDecoder{Decoder: gob.NewDecoder(buf), Count: i}
		err = obj2.decodeFrom(dec)
		assert.Error(t, err)
	}
}

func TestAltLeafUn_Indexes(t *testing.T) {
	ctx := db.NewContext(nil)
	obj := createMaximalSampleAltLeafUn(ctx)
	err := obj.Save(ctx)
	assert.NoError(t, err)
	defer deleteSampleAltLeafUn(ctx, obj)

	var obj2 *AltLeafUn
	obj2, _ = LoadAltLeafUnByAltRootUnID(ctx, obj.AltRootUnID())
	assert.Equal(t, obj.PrimaryKey(), obj2.PrimaryKey())
	assert.True(t, func() bool { h, _ := HasAltLeafUnByAltRootUnID(ctx, obj.AltRootUnID()); return h }())

}
