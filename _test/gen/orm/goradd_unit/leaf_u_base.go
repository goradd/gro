// Code generated by goradd-orm. DO NOT EDIT.

package goradd_unit

import (
	"bytes"
	"context"
	"encoding/gob"
	"encoding/json"
	"fmt"
	"unicode/utf8"

	"github.com/goradd/anyutil"
	"github.com/goradd/orm/_test/gen/orm/goradd_unit/node"
	"github.com/goradd/orm/pkg/broadcast"
	"github.com/goradd/orm/pkg/db"
	"github.com/goradd/orm/pkg/op"
	"github.com/goradd/orm/pkg/query"
)

// LeafUBase is embedded in a LeafU object and provides the ORM access to the database.
// The member variables of the structure are private and should not normally be accessed by the LeafU embedder.
// Instead, use the accessor functions.
type leafUBase struct {
	id              string
	idIsLoaded      bool
	idIsDirty       bool
	name            string
	nameIsLoaded    bool
	nameIsDirty     bool
	rootUID         string
	rootUIDIsLoaded bool
	rootUIDIsDirty  bool
	objRootU        *RootU

	// Custom aliases, if specified
	_aliases map[string]any

	// Indicates whether this is a new object, or one loaded from the database. Used by Save to know whether to Insert or Update.
	_restored bool

	_originalPK string
}

// IDs used to access the LeafU object fields by name using the Get function.
// doc: type=LeafU
const (
	LeafU_ID      = `ID`
	LeafU_Name    = `Name`
	LeafU_RootUID = `RootUID`
	LeafU_RootU   = `RootU`
)

const LeafUIDMaxLength = 32    // The number of runes the column can hold
const LeafUNameMaxLength = 100 // The number of runes the column can hold

// Initialize or re-initialize a LeafU database object to default values.
// The primary key will get a temporary negative number which will be replaced when the object is saved.
// Multiple calls to Initialize are not guaranteed to create sequential values for the primary key.
func (o *leafUBase) Initialize() {
	o.id = db.TemporaryPrimaryKey()
	o.idIsLoaded = true
	o.idIsDirty = false

	o.name = ""
	o.nameIsLoaded = false
	o.nameIsDirty = false

	o.rootUID = ""
	o.rootUIDIsLoaded = false
	o.rootUIDIsDirty = false

	o._aliases = nil
	o._restored = false
}

// PrimaryKey returns the current value of the primary key field.
func (o *leafUBase) PrimaryKey() string {
	return o.id
}

// OriginalPrimaryKey returns the value of the primary key that was originally loaded into the object when it was
// read from the database.
func (o *leafUBase) OriginalPrimaryKey() string {
	return o._originalPK
}

// Copy copies most fields to a new LeafU object.
// Forward reference ids will be copied, but reverse and many-many references will not.
// Attached objects will not be included in the copy.
// Automatically generated fields will not be included in the copy.
// The primary key field will not be copied, since it is normally auto-generated.
// Call Save() on the new object to save it into the database.
// Copy might panic if any fields in the database were set to a size larger than the
// maximum size through a process that accessed the database outside of the ORM.
func (o *leafUBase) Copy() (newObject *LeafU) {
	newObject = NewLeafU()
	if o.idIsLoaded {
		newObject.SetID(o.id)
	}
	if o.nameIsLoaded {
		newObject.SetName(o.name)
	}
	if o.rootUIDIsLoaded {
		newObject.SetRootUID(o.rootUID)
	}
	return
}

// ID returns the value of ID.
func (o *leafUBase) ID() string {
	if o._restored && !o.idIsLoaded {
		panic("ID was not selected in the last query and has not been set, and so is not valid")
	}
	return o.id
}

// IDIsLoaded returns true if the value was loaded from the database or has been set.
func (o *leafUBase) IDIsLoaded() bool {
	return o.idIsLoaded
}

// SetID sets the value of ID in the object, to be saved later in the database using the Save() function.
// Normally you will not need to call this function, since the ID value is automatically generated by the
// database driver. Exceptions might include importing data to a new database, or correcting primary key conflicts when
// merging data.
// You cannot change a primary key for a record that has been written to the database. While SQL databases will
// allow it, NoSql databases will not. Save a copy and delete this one instead.
func (o *leafUBase) SetID(v string) {
	if o._restored {
		panic("error: Do not change a primary key for a record that has been saved. Instead, save a copy and delete the original.")
	}
	if utf8.RuneCountInString(v) > LeafUIDMaxLength {
		panic("attempted to set LeafU.ID to a value larger than its maximum length in runes")
	}

	o.idIsLoaded = true
	o.id = v
	o.idIsDirty = true
}

// Name returns the value of Name.
func (o *leafUBase) Name() string {
	if o._restored && !o.nameIsLoaded {
		panic("Name was not selected in the last query and has not been set, and so is not valid")
	}
	return o.name
}

// NameIsLoaded returns true if the value was loaded from the database or has been set.
func (o *leafUBase) NameIsLoaded() bool {
	return o.nameIsLoaded
}

// SetName sets the value of Name in the object, to be saved later in the database using the Save() function.
func (o *leafUBase) SetName(v string) {
	if utf8.RuneCountInString(v) > LeafUNameMaxLength {
		panic("attempted to set LeafU.Name to a value larger than its maximum length in runes")
	}
	if o._restored &&
		o.nameIsLoaded && // if it was not selected, then make sure it gets set, since our end comparison won't be valid
		o.name == v {
		// no change
		return
	}

	o.nameIsLoaded = true
	o.name = v
	o.nameIsDirty = true
}

// RootUID returns the value of RootUID.
func (o *leafUBase) RootUID() string {
	if o._restored && !o.rootUIDIsLoaded {
		panic("RootUID was not selected in the last query and has not been set, and so is not valid")
	}
	return o.rootUID
}

// RootUIDIsLoaded returns true if the value was loaded from the database or has been set.
func (o *leafUBase) RootUIDIsLoaded() bool {
	return o.rootUIDIsLoaded
}

// SetRootUID sets the value of RootUID in the object, to be saved later in the database using the Save() function.
func (o *leafUBase) SetRootUID(v string) {
	if o._restored &&
		o.rootUIDIsLoaded && // if it was not selected, then make sure it gets set, since our end comparison won't be valid
		o.rootUID == v {
		// no change
		return
	}

	o.rootUIDIsLoaded = true
	o.rootUID = v
	o.rootUIDIsDirty = true
	if o.objRootU != nil &&
		o.rootUID != o.objRootU.PrimaryKey() {
		o.objRootU = nil
	}
}

// RootU returns the current value of the loaded RootU, and nil if its not loaded.
func (o *leafUBase) RootU() *RootU {
	return o.objRootU
}

// LoadRootU returns the related RootU. If it is not already loaded,
// it will attempt to load it, provided the RootUID column has been loaded first.
func (o *leafUBase) LoadRootU(ctx context.Context) (*RootU, error) {
	var err error

	if o.objRootU == nil {
		if !o.rootUIDIsLoaded {
			panic("RootUID must be selected in the previous query")
		}
		// Load and cache
		o.objRootU, err = LoadRootU(ctx, o.rootUID)
	}
	return o.objRootU, err
}

// SetRootU sets the value of RootU in the object, to be saved later using the Save() function.
func (o *leafUBase) SetRootU(objRootU *RootU) {
	if objRootU == nil {
		panic("Cannot set RootU to a nil value since RootUID is not nullable.")
	} else {
		o.objRootU = objRootU
		o.rootUIDIsLoaded = true
		if o.rootUID != objRootU.PrimaryKey() {
			o.rootUID = objRootU.PrimaryKey()
			o.rootUIDIsDirty = true
		}
	}
}

// GetAlias returns the value for the Alias node aliasKey that was returned in the most
// recent query.
func (o *leafUBase) GetAlias(aliasKey string) query.AliasValue {
	if a, ok := o._aliases[aliasKey]; ok {
		return query.NewAliasValue(a)
	} else {
		panic("Alias " + aliasKey + " not found.")
	}
}

// IsNew returns true if the object will create a new record when saved.
func (o *leafUBase) IsNew() bool {
	return !o._restored
}

// LoadLeafU returns a LeafU from the database.
// selectNodes lets you provide nodes for selecting specific fields or additional fields from related tables.
// See [LeafUsBuilder.Select] for more info.
func LoadLeafU(ctx context.Context, id string, selectNodes ...query.Node) (*LeafU, error) {
	return queryLeafUs(ctx).
		Where(op.Equal(node.LeafU().ID(), id)).
		Select(selectNodes...).
		Get()
}

// HasLeafU returns true if a LeafU with the given primary key exists in the database.
// doc: type=LeafU
func HasLeafU(ctx context.Context, id string) (bool, error) {
	v, err := queryLeafUs(ctx).
		Where(op.Equal(node.LeafU().ID(), id)).
		Count()
	return v > 0, err
}

// LoadLeafUByRootUID queries for a single LeafU object by the given unique index values.
// selectNodes optionally let you provide nodes for joining to other tables or selecting specific fields.
// See [LeafUsBuilder.Select].
// If you need a more elaborate query, use QueryLeafUs() to start a query builder.
func LoadLeafUByRootUID(ctx context.Context, rootUID string, selectNodes ...query.Node) (*LeafU, error) {
	q := queryLeafUs(ctx)
	q = q.Where(op.Equal(node.LeafU().RootUID(), rootUID))
	return q.Select(selectNodes...).Get()
}

// HasLeafUByRootUID returns true if the
// given unique index values exist in the database.
// doc: type=LeafU
func HasLeafUByRootUID(ctx context.Context, rootUID string) (bool, error) {
	q := queryLeafUs(ctx)
	q = q.Where(op.Equal(node.LeafU().RootUID(), rootUID))
	v, err := q.Count()
	return v > 0, err
}

// The LeafUBuilder uses a builder pattern to create a query on the database.
// Create a LeafUBuilder by calling QueryLeafUs, which will select all
// the LeafU object in the database. Then filter and arrange those objects
// by calling Where, Select, etc.
// End a query by calling either Load, LoadI, LoadCursor, Get, or Count.
// A LeafUBuilder stores the context it will use to perform the query, and thus is
// meant to be a short-lived object. You should not save it for later use.
type LeafUBuilder struct {
	builder *query.Builder
	ctx     context.Context
}

func newLeafUBuilder(ctx context.Context) *LeafUBuilder {
	b := LeafUBuilder{
		builder: query.NewBuilder(node.LeafU()),
		ctx:     ctx,
	}
	return &b
}

// Load terminates the query builder, performs the query, and returns a slice of LeafU objects.
// If there are any errors, nil is returned and the specific error is stored in the context.
// If no results come back from the query, it will return a non-nil empty slice.
func (b *LeafUBuilder) Load() (leafUs []*LeafU, err error) {
	b.builder.Command = query.BuilderCommandLoad
	database := db.GetDatabase("goradd_unit")
	var results any

	ctx := b.ctx
	results, err = database.BuilderQuery(ctx, b.builder)
	if results == nil || err != nil {
		return
	}
	for _, item := range results.([]map[string]any) {
		o := new(LeafU)
		o.unpack(item, o)
		leafUs = append(leafUs, o)
	}
	return
}

// LoadI terminates the query builder, performs the query, and returns a slice of interfaces.
// This can then satisfy a variety of interfaces that load arrays of objects, including KeyLabeler.
// If there are any errors, nil is returned and the specific error is stored in the context.
// If no results come back from the query, it will return a non-nil empty slice.
func (b *LeafUBuilder) LoadI() (leafUs []query.OrmObj, err error) {
	b.builder.Command = query.BuilderCommandLoad
	database := db.GetDatabase("goradd_unit")
	var results any

	ctx := b.ctx
	results, err = database.BuilderQuery(ctx, b.builder)
	if results == nil || err != nil {
		return
	}
	for _, item := range results.([]map[string]any) {
		o := new(LeafU)
		o.unpack(item, o)
		leafUs = append(leafUs, o)
	}
	return
}

// LoadCursor terminates the query builder, performs the query, and returns a cursor to the query.
//
// A query cursor is useful for dealing with large amounts of query results. However, there are some
// limitations to its use. When working with SQL databases, you cannot use a cursor while querying
// many-to-many or reverse relationships that will create an array of values.
//
// Call Next() on the returned cursor object to step through the results. Make sure you call Close
// on the cursor object when you are done. You should use
//
//	defer cursor.Close()
//
// to make sure the cursor gets closed.
func (b *LeafUBuilder) LoadCursor() (leafUsCursor, error) {
	b.builder.Command = query.BuilderCommandLoadCursor
	database := db.GetDatabase("goradd_unit")
	result, err := database.BuilderQuery(b.ctx, b.builder)
	var cursor query.CursorI
	if result != nil {
		cursor = result.(query.CursorI)
	}
	return leafUsCursor{cursor}, err
}

type leafUsCursor struct {
	query.CursorI
}

// Next returns the current LeafU object and moves the cursor to the next one.
//
// If there are no more records, it returns nil.
func (c leafUsCursor) Next() (*LeafU, error) {
	if c.CursorI == nil {
		return nil, nil
	}

	row, err := c.CursorI.Next()
	if row == nil || err != nil {
		return nil, err
	}
	o := new(LeafU)
	o.unpack(row, o)
	return o, nil
}

// Get is a convenience method to return only the first item found in a query.
// The entire query is performed, so you should generally use this only if you know
// you are selecting on one or very few items.
// If an error occurs, or no results are found, a nil is returned.
func (b *LeafUBuilder) Get() (*LeafU, error) {
	results, err := b.Load()
	if err != nil || len(results) == 0 {
		return nil, err
	}
	return results[0], nil
}

// Where adds a condition to filter what gets selected.
// Calling Where multiple times will AND the conditions together.
func (b *LeafUBuilder) Where(c query.Node) *LeafUBuilder {
	b.builder.Where(c)
	return b
}

// OrderBy specifies how the resulting data should be sorted.
// By default, the given nodes are sorted in ascending order.
// Add Descending() to the node to specify that it should be sorted in descending order.
func (b *LeafUBuilder) OrderBy(nodes ...query.Sorter) *LeafUBuilder {
	b.builder.OrderBy(nodes...)
	return b
}

// Limit will return a subset of the data, limited to the offset and number of rows specified.
// For large data sets and specific types of queries, this can be slow, because it will perform
// the entire query before computing the limit.
// You cannot limit a query that has embedded arrays.
func (b *LeafUBuilder) Limit(maxRowCount int, offset int) *LeafUBuilder {
	b.builder.Limit(maxRowCount, offset)
	return b
}

// Select specifies what specific columns will be loaded with data.
// By default, all the columns of the leaf_u table will be queried and loaded.
// If nodes contains columns from the leaf_u table, that will limit the columns queried and loaded to only those columns.
// If related tables are specified, then all the columns from those tables are queried, selected and joined to the result.
// If columns in related tables are specified, then only those columns will be queried and loaded.
// Depending on the query, additional columns may automatically be added to the query. In particular, primary key columns
// will be added in most situations. The exception to this would be in distinct queries, group by queries, or subqueries.
func (b *LeafUBuilder) Select(nodes ...query.Node) *LeafUBuilder {
	b.builder.Select(nodes...)
	return b
}

// Calculation adds operation as an aliased value onto base.
// After the query, you can read the data by passing alias to GetAlias on the returned object.
func (b *LeafUBuilder) Calculation(base query.TableNodeI, alias string, operation query.OperationNodeI) *LeafUBuilder {
	b.builder.Calculation(base, alias, operation)
	return b
}

// Distinct removes duplicates from the results of the query.
// Adding a Select() is usually required.
func (b *LeafUBuilder) Distinct() *LeafUBuilder {
	b.builder.Distinct()
	return b
}

// GroupBy controls how results are grouped when using aggregate functions with Calculation.
func (b *LeafUBuilder) GroupBy(nodes ...query.Node) *LeafUBuilder {
	b.builder.GroupBy(nodes...)
	return b
}

// Having does additional filtering on the results of the query after the query is performed.
func (b *LeafUBuilder) Having(node query.Node) *LeafUBuilder {
	b.builder.Having(node)
	return b
}

// Count terminates a query and returns just the number of items in the result.
// If you have Select or Calculation columns in the query, it will count NULL results as well.
// To not count NULL values, use Where in the builder with a NotNull operation.
// To count distinct combinations of items, call Distinct() on the builder.
func (b *LeafUBuilder) Count() (int, error) {
	b.builder.Command = query.BuilderCommandCount
	database := db.GetDatabase("goradd_unit")

	ctx := b.ctx
	results, err := database.BuilderQuery(ctx, b.builder)
	if results == nil || err != nil {
		return 0, err
	}
	return results.(int), nil
}

// CountLeafUs returns the total number of items in the leaf_u table.
func CountLeafUs(ctx context.Context) (int, error) {
	return QueryLeafUs(ctx).Count()
}

// CountLeafUsByRootUID queries the database and returns the number of LeafU objects that
// have rootUID.
// doc: type=LeafU
func CountLeafUsByRootUID(ctx context.Context, rootUID string) (int, error) {
	v_rootUID := rootUID
	return QueryLeafUs(ctx).
		Where(op.Equal(node.LeafU().RootUID(), v_rootUID)).
		Count()
}

// unpack recursively transforms data coming from the database into ORM objects.
func (o *leafUBase) unpack(m map[string]interface{}, objThis *LeafU) {

	if v, ok := m["id"]; ok && v != nil {
		if o.id, ok = v.(string); ok {
			o.idIsLoaded = true
			o.idIsDirty = false

			o._originalPK = o.id

		} else {
			panic("Wrong type found for id.")
		}
	} else {
		o.idIsLoaded = false
		o.id = ""
		o.idIsDirty = false
	}

	if v, ok := m["name"]; ok && v != nil {
		if o.name, ok = v.(string); ok {
			o.nameIsLoaded = true
			o.nameIsDirty = false

		} else {
			panic("Wrong type found for name.")
		}
	} else {
		o.nameIsLoaded = false
		o.name = ""
		o.nameIsDirty = false
	}

	if v, ok := m["root_u_id"]; ok && v != nil {
		if o.rootUID, ok = v.(string); ok {
			o.rootUIDIsLoaded = true
			o.rootUIDIsDirty = false

		} else {
			panic("Wrong type found for root_u_id.")
		}
	} else {
		o.rootUIDIsLoaded = false
		o.rootUID = ""
		o.rootUIDIsDirty = false
	}

	if v, ok := m["RootU"]; ok {
		if objRootU, ok2 := v.(map[string]any); ok2 {
			o.objRootU = new(RootU)
			o.objRootU.unpack(objRootU, o.objRootU)
			o.rootUIDIsLoaded = true
			o.rootUIDIsDirty = false
		} else {
			panic("Wrong type found for RootU object.")
		}
	} else {
		o.objRootU = nil
	}

	if v, ok := m["aliases_"]; ok {
		o._aliases = v.(map[string]any)
	}

	o._restored = true

}

// save will update or insert the object, depending on the state of the object.
func (o *leafUBase) save(ctx context.Context) error {
	if o._restored {
		return o.update(ctx)
	} else {
		return o.insert(ctx)
	}
}

// update will update the values in the database, saving any changed values.
// If the table has auto-generated values, those will be updated automatically.
func (o *leafUBase) update(ctx context.Context) error {
	if !o._restored {
		panic("cannot update a record that was not originally read from the database.")
	}
	if !o.IsDirty() {
		return nil // nothing to save
	}

	var modifiedFields map[string]interface{}

	d := Database()
	err := db.ExecuteTransaction(ctx, d, func() error {

		// Save loaded RootU object to get its new pk and update it here.
		if o.objRootU != nil {
			if err := o.objRootU.Save(ctx); err != nil {
				return err
			}
			o.SetRootUID(o.objRootU.PrimaryKey())
		}

		if o.rootUIDIsDirty {
			if obj, err := LoadLeafUByRootUID(ctx, o.rootUID); err != nil {
				return err
			} else if obj != nil {
				return db.NewUniqueValueError("leaf_u", map[string]any{"root_u_id": o.rootUID}, nil)
			}
		}

		modifiedFields = getLeafUUpdateFields(o)
		if len(modifiedFields) != 0 {
			var err2 error

			_, err2 = d.Update(ctx, "leaf_u", "id", o._originalPK, modifiedFields, "", 0)
			if err2 != nil {
				return err2
			}
		}

		return nil
	}) // transaction
	if err != nil {
		return err
	}

	o.resetDirtyStatus()
	if len(modifiedFields) != 0 {
		broadcast.Update(ctx, "goradd_unit", "leaf_u", o._originalPK, anyutil.SortedKeys(modifiedFields)...)
	}

	return nil
}

// insert will insert the object into the database. Related items will be saved.
func (o *leafUBase) insert(ctx context.Context) (err error) {
	var insertFields map[string]interface{}
	d := Database()
	err = db.ExecuteTransaction(ctx, d, func() error {

		// Save loaded RootU object to get its new pk and update it here.
		if o.objRootU != nil {
			if err := o.objRootU.Save(ctx); err != nil {
				return err
			}
			o.SetRootUID(o.objRootU.PrimaryKey())
		}

		if !o.nameIsLoaded {
			panic("a value for Name is required, and there is no default value. Call SetName() before inserting the record.")
		}
		if !o.rootUIDIsLoaded {
			panic("a value for RootUID is required, and there is no default value. Call SetRootUID() before inserting the record.")
		}
		if o.rootUIDIsDirty {
			if obj, err := LoadLeafUByRootUID(ctx, o.rootUID); err != nil {
				return err
			} else if obj != nil {
				return db.NewUniqueValueError("leaf_u", map[string]any{"root_u_id": o.rootUID}, nil)
			}
		}
		insertFields = getLeafUInsertFields(o)
		var newPK string
		newPK, err = d.Insert(ctx, "leaf_u", "id", insertFields)
		if err != nil {
			return err
		}
		o.id = newPK
		o._originalPK = newPK
		o.idIsLoaded = true

		return nil

	}) // transaction

	if err != nil {
		return
	}

	o.resetDirtyStatus()
	o._restored = true
	broadcast.Insert(ctx, "goradd_unit", "leaf_u", o.PrimaryKey())
	return
}

// getUpdateFields returns the database columns that will be sent to the update process.
// This will include timestamp fields only if some other column has changed.
func (o *leafUBase) getUpdateFields() (fields map[string]interface{}) {
	fields = map[string]interface{}{}
	if o.idIsDirty {
		fields["id"] = o.id
	}
	if o.nameIsDirty {
		fields["name"] = o.name
	}
	if o.rootUIDIsDirty {
		fields["root_u_id"] = o.rootUID
	}
	return
}

// getInsertFields returns the fields that will be specified in an insert operation.
// Optional fields that have not been set and have no default will be returned as nil.
// NoSql databases should interpret this as no value. Sql databases should interpret this as
// explicitly setting a NULL value, which would override any database specific default value.
// Auto-generated fields will be returned with their generated values, except AutoPK fields, which are generated by the
// database driver and updated after the insert.
func (o *leafUBase) getInsertFields() (fields map[string]interface{}) {
	fields = map[string]interface{}{}
	if o.idIsDirty {
		fields["id"] = o.id
	}

	fields["name"] = o.name

	fields["root_u_id"] = o.rootUID
	return
}

// Delete deletes the record from the database.
func (o *leafUBase) Delete(ctx context.Context) (err error) {
	if o == nil {
		return // allow deleting of a nil object to be a noop
	}
	if !o._restored {
		panic("Cannot delete a record that has no primary key value.")
	}
	d := Database()
	err = d.Delete(ctx, "leaf_u", "ID", o.id, "", 0)
	if err != nil {
		return err
	}
	broadcast.Delete(ctx, "goradd_unit", "leaf_u", fmt.Sprint(o.id))
	return
}

// deleteLeafU deletes the LeafU with primary key pk from the database
// and handles associated records.
func deleteLeafU(ctx context.Context, pk string) error {
	d := db.GetDatabase("goradd_unit")
	err := d.Delete(ctx, "leaf_u", "id", pk, "", 0)
	if err != nil {
		return err
	}
	broadcast.Delete(ctx, "goradd_unit", "leaf_u", fmt.Sprint(pk))
	return err
}

// resetDirtyStatus resets the dirty status of every field in the object.
func (o *leafUBase) resetDirtyStatus() {
	o.idIsDirty = false
	o.nameIsDirty = false
	o.rootUIDIsDirty = false

}

// IsDirty returns true if the object has been changed since it was read from the database or created.
// However, a new object that has a column with a default value will be automatically marked as dirty upon creation.
func (o *leafUBase) IsDirty() (dirty bool) {
	dirty = o.idIsDirty ||
		o.nameIsDirty ||
		o.rootUIDIsDirty ||
		(o.objRootU != nil && o.objRootU.IsDirty())

	return
}

// Get returns the value of a field in the object based on the field's name.
// It will also get related objects if they are loaded.
// Invalid fields and objects are returned as nil.
// Get can be used to retrieve a value by using the Identifier of a node.
func (o *leafUBase) Get(key string) interface{} {

	switch key {

	case "ID":
		if !o.idIsLoaded {
			return nil
		}
		return o.id

	case "Name":
		if !o.nameIsLoaded {
			return nil
		}
		return o.name

	case "RootUID":
		if !o.rootUIDIsLoaded {
			return nil
		}
		return o.rootUID

	case "RootU":
		return o.RootU()

	}
	return nil
}

// MarshalBinary serializes the object into a buffer that is deserializable using UnmarshalBinary.
// It should be used for transmitting database objects over the wire, or for temporary storage. It does not send
// a version number, so if the data format changes, its up to you to invalidate the old stored objects.
// The framework uses this to serialize the object when it is stored in a control.
func (o *leafUBase) MarshalBinary() ([]byte, error) {
	buf := new(bytes.Buffer)
	enc := gob.NewEncoder(buf)
	if err := o.encodeTo(enc); err != nil {
		return nil, err
	}
	return buf.Bytes(), nil
}

func (o *leafUBase) encodeTo(enc db.Encoder) error {

	if err := enc.Encode(o.id); err != nil {
		return fmt.Errorf("error encoding LeafU.id: %w", err)
	}
	if err := enc.Encode(o.idIsLoaded); err != nil {
		return fmt.Errorf("error encoding LeafU.idIsLoaded: %w", err)
	}
	if err := enc.Encode(o.idIsDirty); err != nil {
		return fmt.Errorf("error encoding LeafU.idIsDirty: %w", err)
	}

	if err := enc.Encode(o.name); err != nil {
		return fmt.Errorf("error encoding LeafU.name: %w", err)
	}
	if err := enc.Encode(o.nameIsLoaded); err != nil {
		return fmt.Errorf("error encoding LeafU.nameIsLoaded: %w", err)
	}
	if err := enc.Encode(o.nameIsDirty); err != nil {
		return fmt.Errorf("error encoding LeafU.nameIsDirty: %w", err)
	}

	if err := enc.Encode(o.rootUID); err != nil {
		return fmt.Errorf("error encoding LeafU.rootUID: %w", err)
	}
	if err := enc.Encode(o.rootUIDIsLoaded); err != nil {
		return fmt.Errorf("error encoding LeafU.rootUIDIsLoaded: %w", err)
	}
	if err := enc.Encode(o.rootUIDIsDirty); err != nil {
		return fmt.Errorf("error encoding LeafU.rootUIDIsDirty: %w", err)
	}

	if o.objRootU == nil {
		if err := enc.Encode(false); err != nil {
			return err
		}
	} else {
		if err := enc.Encode(true); err != nil {
			return err
		}
		if err := enc.Encode(o.objRootU); err != nil {
			return fmt.Errorf("error encoding LeafU.objRootU: %w", err)
		}
	}

	if o._aliases == nil {
		if err := enc.Encode(false); err != nil {
			return err
		}
	} else {
		if err := enc.Encode(true); err != nil {
			return err
		}
		if err := enc.Encode(o._aliases); err != nil {
			return fmt.Errorf("error encoding LeafU._aliases: %w", err)
		}
	}

	if err := enc.Encode(o._restored); err != nil {
		return fmt.Errorf("error encoding LeafU._restored: %w", err)
	}
	if err := enc.Encode(o._originalPK); err != nil {
		return fmt.Errorf("error encoding LeafU._originalPK: %w", err)
	}
	return nil
}

// UnmarshalBinary converts a structure that was created with MarshalBinary into a LeafU object.
func (o *leafUBase) UnmarshalBinary(data []byte) (err error) {
	buf := bytes.NewReader(data)
	dec := gob.NewDecoder(buf)
	return o.decodeFrom(dec)
}

func (o *leafUBase) decodeFrom(dec db.Decoder) (err error) {
	var isPtr bool

	_ = isPtr
	if err = dec.Decode(&o.id); err != nil {
		return fmt.Errorf("error decoding LeafU.id: %w", err)
	}
	if err = dec.Decode(&o.idIsLoaded); err != nil {
		return fmt.Errorf("error decoding LeafU.idIsLoaded: %w", err)
	}
	if err = dec.Decode(&o.idIsDirty); err != nil {
		return fmt.Errorf("error decoding LeafU.idIsDirty: %w", err)
	}

	if err = dec.Decode(&o.name); err != nil {
		return fmt.Errorf("error decoding LeafU.name: %w", err)
	}
	if err = dec.Decode(&o.nameIsLoaded); err != nil {
		return fmt.Errorf("error decoding LeafU.nameIsLoaded: %w", err)
	}
	if err = dec.Decode(&o.nameIsDirty); err != nil {
		return fmt.Errorf("error decoding LeafU.nameIsDirty: %w", err)
	}

	if err = dec.Decode(&o.rootUID); err != nil {
		return fmt.Errorf("error decoding LeafU.rootUID: %w", err)
	}
	if err = dec.Decode(&o.rootUIDIsLoaded); err != nil {
		return fmt.Errorf("error decoding LeafU.rootUIDIsLoaded: %w", err)
	}
	if err = dec.Decode(&o.rootUIDIsDirty); err != nil {
		return fmt.Errorf("error decoding LeafU.rootUIDIsDirty: %w", err)
	}

	if err = dec.Decode(&isPtr); err != nil {
		return fmt.Errorf("error decoding LeafU.objRootU isPtr: %w", err)
	}
	if isPtr {
		if err = dec.Decode(&o.objRootU); err != nil {
			return fmt.Errorf("error decoding LeafU.objRootU: %w", err)
		}
	}
	if err = dec.Decode(&isPtr); err != nil {
		return fmt.Errorf("error decoding LeafU._aliases isPtr: %w", err)
	}
	if isPtr {
		if err = dec.Decode(&o._aliases); err != nil {
			return fmt.Errorf("error decoding LeafU._aliases: %w", err)
		}
	}

	if err = dec.Decode(&o._restored); err != nil {
		return fmt.Errorf("error decoding LeafU._restored: %w", err)
	}
	if err = dec.Decode(&o._originalPK); err != nil {
		return fmt.Errorf("error decoding LeafU._originalPK: %w", err)
	}
	return
}

// MarshalJSON serializes the object into a JSON object.
// Only valid data will be serialized, meaning, you can control what gets serialized by using Select to
// select only the fields you want when you query for the object. Another way to control the output
// is to call MarshalStringMap, modify the map, then encode the map.
func (o *leafUBase) MarshalJSON() (data []byte, err error) {
	v := o.MarshalStringMap()
	return json.Marshal(v)
}

// MarshalStringMap serializes the object into a string map of interfaces.
// Only valid data will be serialized, meaning, you can control what gets serialized by using Select to
// select only the fields you want when you query for the object. The keys are the same as the json keys.
func (o *leafUBase) MarshalStringMap() map[string]interface{} {
	v := make(map[string]interface{})

	if o.idIsLoaded {
		v["id"] = o.id
	}

	if o.nameIsLoaded {
		v["name"] = o.name
	}

	if val := o.objRootU; val != nil {
		v["rootU"] = val.MarshalStringMap()
	} else if o.rootUIDIsLoaded {
		v["rootUID"] = o.rootUID
	}

	for _k, _v := range o._aliases {
		v[_k] = _v
	}
	return v
}

// UnmarshalJSON unmarshalls the given json data into the LeafU. The LeafU can be a
// newly created object, or one loaded from the database.
//
// After unmarshalling, the object is not  saved. You must call Save to insert it into the database
// or update it.
//
// Unmarshalling of sub-objects, as in objects linked via foreign keys, is not currently supported.
//
// The fields it expects are:
//
//	"id" - string
//	"name" - string
//	"rootUID" - string
func (o *leafUBase) UnmarshalJSON(data []byte) (err error) {
	var v map[string]interface{}
	if len(data) == 0 {
		return
	}
	d := json.NewDecoder(bytes.NewReader(data))
	d.UseNumber() // use a number to avoid precision errors
	if err = d.Decode(&v); err != nil {
		return err
	}
	return o.UnmarshalStringMap(v)
}

// UnmarshalStringMap will load the values from the stringmap into the object.
//
// Override this in LeafU to modify the json before sending it here.
func (o *leafUBase) UnmarshalStringMap(m map[string]interface{}) (err error) {
	for k, v := range m {
		switch k {

		case "id":
			{
				if v == nil {
					return fmt.Errorf("field %s cannot be null", k)
				}

				if s, ok := v.(string); !ok {
					return fmt.Errorf("json field %s must be a string", k)
				} else {
					o.SetID(s)
				}
			}

		case "name":
			{
				if v == nil {
					return fmt.Errorf("field %s cannot be null", k)
				}

				if s, ok := v.(string); !ok {
					return fmt.Errorf("json field %s must be a string", k)
				} else {
					o.SetName(s)
				}
			}

		case "rootUID":
			{
				if v == nil {
					return fmt.Errorf("field %s cannot be null", k)
				}

				if _, ok := m["rootU"]; ok {
					continue // importing the foreign key will remove the object
				}

				if s, ok := v.(string); !ok {
					return fmt.Errorf("json field %s must be a string", k)
				} else {
					o.SetRootUID(s)
				}
			}

		case "rootU":
			v2 := NewRootU()
			m2, ok := v.(map[string]any)
			if !ok {
				return fmt.Errorf("json field %s must be a map", k)
			}
			err = v2.UnmarshalStringMap(m2)
			if err != nil {
				return
			}
			o.SetRootU(v2)

		}
	}
	return
}
