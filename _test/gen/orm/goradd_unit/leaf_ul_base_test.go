// Code generated by goradd-orm. DO NOT EDIT.

package goradd_unit

import (
	"bytes"
	"context"
	"encoding/gob"
	"encoding/json"
	"strconv"
	"testing"

	"github.com/goradd/orm/_test/gen/orm/goradd_unit/node"
	"github.com/goradd/orm/pkg/op"
	"github.com/goradd/orm/pkg/test"
	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
)

// createMinimalSampleLeafUl creates an unsaved minimal version of a LeafUl object
// for testing.
func createMinimalSampleLeafUl() *LeafUl {
	obj := NewLeafUl()
	updateMinimalSampleLeafUl(obj)

	// A required forward reference will need to be fulfilled just to save the minimal version of this object
	// If the database is configured so that the referenced object points back here, either directly or through multiple
	// forward references, it possible this could create an endless loop.
	obj.SetRootUl(createMinimalSampleRootUl())

	return obj
}

// updateMinimalSampleLeafUl sets the values of a minimal sample to new, random values.
func updateMinimalSampleLeafUl(obj *LeafUl) {

	obj.SetName(test.RandomValue[string](100))

}

// createMaximalSampleLeafUl creates an unsaved version of a LeafUl object
// for testing that includes references to minimal objects.
func createMaximalSampleLeafUl(ctx context.Context) *LeafUl {
	obj := NewLeafUl()
	updateMaximalSampleLeafUl(ctx, obj)
	return obj
}

// updateMaximalSampleLeafUl sets all the maximal sample values to new values.
// This will set new values for references, so save the old values and delete them.
func updateMaximalSampleLeafUl(ctx context.Context, obj *LeafUl) {
	updateMinimalSampleLeafUl(obj)
	obj.SetRootUl(createMinimalSampleRootUl())

}

// deleteSampleLeafUl deletes an object created and saved by one of the sample creator functions.
func deleteSampleLeafUl(ctx context.Context, obj *LeafUl) {
	if obj == nil {
		return
	}

	_ = obj.Delete(ctx)
	deleteSampleRootUl(ctx, obj.RootUl())
}

// assertEqualFieldsLeafUl compares two objects and asserts that the basic fields are equal.
func assertEqualFieldsLeafUl(t *testing.T, obj1, obj2 *LeafUl) {
	if obj1.IDIsLoaded() && obj2.IDIsLoaded() { // only check loaded values
		assert.EqualValues(t, obj1.ID(), obj2.ID())
	}
	if obj1.NameIsLoaded() && obj2.NameIsLoaded() { // only check loaded values
		assert.EqualValues(t, obj1.Name(), obj2.Name())
	}
	if obj1.GroLockIsLoaded() && obj2.GroLockIsLoaded() { // only check loaded values
		assert.EqualValues(t, obj1.GroLock(), obj2.GroLock())
	}

}

func TestLeafUl_SetID(t *testing.T) {

	obj := NewLeafUl()

	assert.True(t, obj.IsNew())
	val := test.RandomNumberString()
	obj.SetID(val)
	assert.Equal(t, val, obj.ID())

	// test default
	obj.SetID("")
	assert.EqualValues(t, "", obj.ID(), "set default")

	// test panic on setting value larger than maximum size allowed
	val = test.RandomValue[string](33)
	assert.Panics(t, func() {
		obj.SetID(val)
	})
}
func TestLeafUl_SetName(t *testing.T) {

	obj := NewLeafUl()

	assert.True(t, obj.IsNew())
	val := test.RandomValue[string](100)
	obj.SetName(val)
	assert.Equal(t, val, obj.Name())

	// test default
	obj.SetName("")
	assert.EqualValues(t, "", obj.Name(), "set default")

	// test panic on setting value larger than maximum size allowed
	val = test.RandomValue[string](101)
	assert.Panics(t, func() {
		obj.SetName(val)
	})
}
func TestLeafUl_SetRootUlID(t *testing.T) {

	obj := NewLeafUl()

	assert.True(t, obj.IsNew())
	val := test.RandomValue[string](32)
	obj.SetRootUlID(val)
	assert.Equal(t, val, obj.RootUlID())

	// test default
	obj.SetRootUlID("")
	assert.EqualValues(t, "", obj.RootUlID(), "set default")

	// test panic on setting value larger than maximum size allowed
	val = test.RandomValue[string](33)
	assert.Panics(t, func() {
		obj.SetRootUlID(val)
	})
}

func TestLeafUl_Copy(t *testing.T) {
	obj := createMinimalSampleLeafUl()

	obj2 := obj.Copy()

	assert.Equal(t, obj.Name(), obj2.Name())
	assert.Equal(t, obj.RootUlID(), obj2.RootUlID())

}

func TestLeafUl_BasicInsert(t *testing.T) {
	obj := createMinimalSampleLeafUl()
	ctx := context.Background()
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleLeafUl(ctx, obj)

	// Test retrieval
	obj2, err := LoadLeafUl(ctx, obj.PrimaryKey())
	require.NotNil(t, obj2)
	assert.NoError(t, err)

	assert.Equal(t, obj2.PrimaryKey(), obj2.OriginalPrimaryKey())

	assert.True(t, obj2.IDIsLoaded())
	assert.Panics(t, func() {
		obj2.SetID(obj2.ID())
	})

	assert.True(t, obj2.NameIsLoaded())
	// test that setting it to the same value will not change the dirty bit
	assert.False(t, obj2.nameIsDirty)
	obj2.SetName(obj2.Name())
	assert.False(t, obj2.nameIsDirty)

	assert.True(t, obj2.GroLockIsLoaded())

}

func TestLeafUl_InsertPanics(t *testing.T) {
	obj := createMinimalSampleLeafUl()
	_ = obj
	ctx := context.Background()
	_ = ctx

	obj.rootUl = nil

	obj.nameIsLoaded = false
	assert.Panics(t, func() { obj.Save(ctx) })
	obj.nameIsLoaded = true

	obj.rootUlIDIsLoaded = false
	assert.Panics(t, func() { obj.Save(ctx) })
	obj.rootUlIDIsLoaded = true

}

func TestLeafUl_BasicUpdate(t *testing.T) {
	obj := createMinimalSampleLeafUl()
	ctx := context.Background()
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleLeafUl(ctx, obj)
	updateMinimalSampleLeafUl(obj)
	assert.NoError(t, obj.Save(ctx))
	obj2, err := LoadLeafUl(ctx, obj.PrimaryKey())
	assert.NoError(t, err)

	assert.Equal(t, obj2.ID(), obj.ID(), "Value did not update")
	assert.Equal(t, obj2.Name(), obj.Name(), "Name did not update")
	assert.Equal(t, obj2.GroLock(), obj.GroLock(), "GroLock did not update")
}

func TestLeafUl_ReferenceLoad(t *testing.T) {
	ctx := context.Background()
	obj := createMaximalSampleLeafUl(ctx)
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleLeafUl(ctx, obj)

	// Test that referenced objects were saved and assigned ids
	assert.NotNil(t, obj.RootUl())
	assert.NotEqual(t, '-', obj.RootUl().PrimaryKey()[0])

	// Test lazy loading
	obj2, err := LoadLeafUl(ctx, obj.PrimaryKey())
	assert.NoError(t, err)
	objPkOnly, err2 := LoadLeafUl(ctx, obj.PrimaryKey(),
		node.LeafUl().ID())
	assert.NoError(t, err2)
	_ = obj2 // avoid error if there are no references
	_ = objPkOnly

	assert.Nil(t, obj2.RootUl(), "RootUl is not loaded initially")
	v_RootUl, _ := obj2.LoadRootUl(ctx)
	assert.NotNil(t, v_RootUl)
	assert.Equal(t, v_RootUl.PrimaryKey(), obj2.RootUl().PrimaryKey())
	assert.Equal(t, obj.RootUl().PrimaryKey(), obj2.RootUl().PrimaryKey())
	assert.True(t, obj2.RootUlIDIsLoaded())

	assert.False(t, objPkOnly.RootUlIDIsLoaded())
	assert.Panics(t, func() { _, _ = objPkOnly.LoadRootUl(ctx) })

	assert.Panics(t, func() {
		objPkOnly.SetRootUl(nil)
	})

	// test eager loading
	obj3, _ := LoadLeafUl(ctx, obj.PrimaryKey(), node.LeafUl().ID(),
		node.LeafUl().Name(),
		node.LeafUl().GroLock(),
	)
	_ = obj3 // avoid error if there are no references

	assert.Equal(t, obj2.RootUl().PrimaryKey(), obj3.RootUl().PrimaryKey())

}

func TestLeafUl_ReferenceUpdateNewObjects(t *testing.T) {
	ctx := context.Background()
	obj := createMaximalSampleLeafUl(ctx)
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleLeafUl(ctx, obj)

	obj2, _ := LoadLeafUl(ctx, obj.PrimaryKey())
	updateMaximalSampleLeafUl(ctx, obj2)
	assert.NoError(t, obj2.Save(ctx))
	defer deleteSampleLeafUl(ctx, obj2)

	obj3, _ := LoadLeafUl(ctx, obj2.PrimaryKey(), node.LeafUl().RootUl())
	_ = obj3 // avoid error if there are no references

	assert.Equal(t, obj2.RootUl().PrimaryKey(), obj3.RootUl().PrimaryKey())

}

func TestLeafUl_ReferenceUpdateOldObjects(t *testing.T) {
	ctx := context.Background()
	obj := createMaximalSampleLeafUl(ctx)
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleLeafUl(ctx, obj)

	updateMinimalSampleRootUl(obj.RootUl())

	assert.NoError(t, obj.Save(ctx))

	obj2, _ := LoadLeafUl(ctx, obj.PrimaryKey(), node.LeafUl().ID(),
		node.LeafUl().Name(),
		node.LeafUl().GroLock(),
	)
	_ = obj2 // avoid error if there are no references

	assertEqualFieldsRootUl(t, obj2.RootUl(), obj.RootUl())

}
func TestLeafUl_EmptyPrimaryKeyGetter(t *testing.T) {
	obj := NewLeafUl()

	i, err := strconv.Atoi(obj.ID())
	assert.NoError(t, err)
	assert.True(t, i < 0)
}

func TestLeafUl_Getters(t *testing.T) {
	obj := createMinimalSampleLeafUl()

	i, err := strconv.Atoi(obj.ID())
	assert.NoError(t, err)
	assert.True(t, i < 0)

	ctx := context.Background()
	require.NoError(t, obj.Save(ctx))
	defer deleteSampleLeafUl(ctx, obj)

	has, _ := HasLeafUl(ctx, obj.PrimaryKey())
	assert.True(t, has)

	obj2, _ := LoadLeafUl(ctx, obj.PrimaryKey(),
		node.LeafUl().ID())

	assert.Equal(t, obj.ID(), obj.Get(node.LeafUl().ID().Identifier))
	assert.Equal(t, obj.Name(), obj.Get(node.LeafUl().Name().Identifier))
	assert.Panics(t, func() { obj2.Name() })
	assert.Nil(t, obj2.Get(node.LeafUl().Name().Identifier))
	assert.Equal(t, obj.GroLock(), obj.Get(node.LeafUl().GroLock().Identifier))
	assert.Panics(t, func() { obj2.GroLock() })
	assert.Nil(t, obj2.Get(node.LeafUl().GroLock().Identifier))
}

func TestLeafUl_QueryLoad(t *testing.T) {
	obj := createMinimalSampleLeafUl()
	ctx := context.Background()
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleLeafUl(ctx, obj)

	objs, err := QueryLeafUls(ctx).
		Where(op.Equal(node.LeafUl().ID(), obj.ID())).
		OrderBy(node.LeafUl().ID()). // exercise order by
		Limit(1, 0).                 // exercise limit
		Calculation(node.LeafUl(), "IsTrue", op.Equal("A", "A")).
		Load()
	assert.NoError(t, err)
	assert.Equal(t, obj.PrimaryKey(), objs[0].PrimaryKey())
	assert.True(t, objs[0].GetAlias("IsTrue").Bool())
}
func TestLeafUl_QueryLoadI(t *testing.T) {
	obj := createMinimalSampleLeafUl()
	ctx := context.Background()
	err := obj.Save(ctx)
	assert.NoError(t, err)
	defer deleteSampleLeafUl(ctx, obj)

	objs, _ := QueryLeafUls(ctx).
		Where(op.Equal(node.LeafUl().ID(), obj.ID())).
		LoadI()

	assert.Equal(t, obj.PrimaryKey(), objs[0].Get("Value"))
}
func TestLeafUl_QueryCursor(t *testing.T) {
	obj := createMinimalSampleLeafUl()
	ctx := context.Background()
	assert.NoError(t, obj.Save(ctx))
	defer deleteSampleLeafUl(ctx, obj)

	cursor, err := QueryLeafUls(ctx).
		Where(op.Equal(node.LeafUl().ID(), obj.ID())).
		LoadCursor()
	require.NoError(t, err)
	obj2, err2 := cursor.Next()
	assert.Equal(t, obj.PrimaryKey(), obj2.PrimaryKey())
	require.NoError(t, err2)
	obj2, err2 = cursor.Next()
	assert.Nil(t, obj2)
	require.NoError(t, err2)
	assert.NoError(t, cursor.Close())

	// test empty cursor result
	cursor, err = QueryLeafUls(ctx).
		Where(op.Equal("B", "A")).
		LoadCursor()
	require.NoError(t, err)

	obj2, err = cursor.Next()
	assert.Nil(t, obj2)
	require.NoError(t, err)
	assert.NoError(t, cursor.Close())
}
func TestLeafUl_Count(t *testing.T) {
	ctx := context.Background()
	obj := createMaximalSampleLeafUl(ctx)
	err := obj.Save(ctx)
	assert.NoError(t, err)
	defer deleteSampleLeafUl(ctx, obj)
	assert.Positive(t, func() int { i, _ := CountLeafUls(ctx); return i }())

	// reread in case there are data limitations imposed by the database
	obj2, _ := LoadLeafUl(ctx, obj.PrimaryKey())
	assert.Positive(t,
		func() int {
			i, _ := CountLeafUlsByRootUlID(ctx,
				obj2.RootUlID())
			return i
		}())

}

func TestLeafUl_MarshalJSON(t *testing.T) {
	obj := createMinimalSampleLeafUl()

	b, err := json.Marshal(obj)
	assert.NoError(t, err)

	obj2 := NewLeafUl()
	err = json.Unmarshal(b, &obj2)
	assert.NoError(t, err)

	assertEqualFieldsLeafUl(t, obj, obj2)
}

func TestLeafUl_MarshalBinary(t *testing.T) {
	obj := createMinimalSampleLeafUl()

	b, err := obj.MarshalBinary()
	assert.NoError(t, err)

	obj2 := NewLeafUl()
	err = obj2.UnmarshalBinary(b)
	assert.NoError(t, err)

	assertEqualFieldsLeafUl(t, obj, obj2)
}

func TestLeafUl_FailingMarshalBinary(t *testing.T) {
	obj := createMinimalSampleLeafUl()
	var err error

	for i := 0; i < 11; i++ {
		enc := &test.GobEncoder{Count: i}
		err = obj.encodeTo(enc)
		assert.Error(t, err)
	}
	// do it again with aliases
	obj._aliases = make(map[string]any)
	for i := 0; i < 12; i++ {
		enc := &test.GobEncoder{Count: i}
		err = obj.encodeTo(enc)
		assert.Error(t, err)
	}
}

func TestLeafUl_FailingUnmarshalBinary(t *testing.T) {
	obj := createMinimalSampleLeafUl()
	b, err := obj.MarshalBinary()
	assert.NoError(t, err)
	obj2 := NewLeafUl()
	for i := 0; i < 11; i++ {
		buf := bytes.NewReader(b)
		dec := &test.GobDecoder{Decoder: gob.NewDecoder(buf), Count: i}
		err = obj2.decodeFrom(dec)
		assert.Error(t, err)
	}

	// do it again with aliases
	obj = createMinimalSampleLeafUl()
	obj._aliases = map[string]any{"a": 1}
	b, err = obj.MarshalBinary()
	assert.NoError(t, err)

	obj2 = NewLeafUl()
	for i := 0; i < 12; i++ {
		buf := bytes.NewReader(b)
		dec := &test.GobDecoder{Decoder: gob.NewDecoder(buf), Count: i}
		err = obj2.decodeFrom(dec)
		assert.Error(t, err)
	}
}
