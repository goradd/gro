// Code generated by goradd-orm. DO NOT EDIT.

package goradd_unit

import (
	"bytes"
	"context"
	"encoding/gob"
	"encoding/json"
	"fmt"
	"unicode/utf8"

	"github.com/goradd/anyutil"
	"github.com/goradd/gro/broadcast"
	"github.com/goradd/gro/ci/tests/gen/goradd_unit/node"
	"github.com/goradd/gro/db"
	"github.com/goradd/gro/query"
	"github.com/goradd/gro/query/op"
)

// RootUlBase is embedded in a RootUl object and provides the ORM access to the database.
// The member variables of the structure are private and should not normally be accessed by the RootUl embedder.
// Instead, use the accessor functions.
type rootUlBase struct {
	id              query.AutoPrimaryKey
	idIsLoaded      bool
	idIsDirty       bool
	name            string
	nameIsLoaded    bool
	nameIsDirty     bool
	groLock         int64
	groLockIsLoaded bool

	// Reverse references
	leafUl        *LeafUl
	leafUlIsDirty bool // is a new one being associated

	// Custom aliases, if specified
	_aliases map[string]any

	// Indicates whether this is a new object, or one loaded from the database. Used by Save to know whether to Insert or Update.
	_restored bool

	_originalPK query.AutoPrimaryKey
}

// IDs used to access the RootUl object fields by name using the Get function.
// doc: type=RootUl
const (
	RootUlIDField      = `id`
	RootUlNameField    = `name`
	RootUlGroLockField = `groLock`
	RootUlLeafUlField  = `leafUl`
)

const RootUlNameMaxLength = 100 // The number of runes the column can hold

// Initialize or re-initialize a RootUl database object to default values.
// The primary key will get a temporary unique value which will be replaced when the object is saved.
func (o *rootUlBase) Initialize() {
	o.id = query.TempAutoPrimaryKey()
	o.idIsLoaded = true
	o.idIsDirty = false

	o.name = ""
	o.nameIsLoaded = false
	o.nameIsDirty = false

	o.groLock = 0
	o.groLockIsLoaded = false

	// Reverse reference objects.

	o.leafUl = nil
	o.leafUlIsDirty = false

	o._aliases = nil
	o._restored = false
}

// Copy copies most fields to a new RootUl object.
// Forward reference ids will be copied, but reverse and many-many references will not.
// Attached objects will not be included in the copy.
// Automatically generated fields will not be included in the copy.
// The primary key field will not be copied, since it is normally auto-generated.
// Call Save() on the new object to save it into the database.
// Copy might panic if any fields in the database were set to a size larger than the
// maximum size through a process that accessed the database outside of the ORM.
func (o *rootUlBase) Copy() (newObject *RootUl) {
	newObject = NewRootUl()
	if o.idIsLoaded {
		newObject.SetID(o.id)
	}
	if o.nameIsLoaded {
		newObject.SetName(o.name)
	}
	return
}

// OriginalPrimaryKey returns the value of the primary key that was originally loaded into the object when it was
// read from the database.
func (o *rootUlBase) OriginalPrimaryKey() query.AutoPrimaryKey {
	return o._originalPK
}

// PrimaryKey returns the value of the primary key of the record.
func (o *rootUlBase) PrimaryKey() query.AutoPrimaryKey {
	if o._restored && !o.idIsLoaded {
		panic("ID was not selected in the last query and has not been set, and so PrimaryKey is not valid")
	}
	return o.id
}

// SetPrimaryKey sets the value of the primary key in the object, to be saved later in the database using the Save() function.
// Normally you will not need to call this function, since the primary key value is automatically generated by the
// database driver. Exceptions might include importing data to a new database, or correcting primary key conflicts when
// merging data.
// You cannot change a primary key for a record that has been written to the database. While SQL databases will
// allow it, NoSql databases will not. Save a copy and delete this one instead.
func (o *rootUlBase) SetPrimaryKey(v query.AutoPrimaryKey) {
	o.SetID(v)
}

// ID returns the loaded value of the id field in the database.
func (o *rootUlBase) ID() query.AutoPrimaryKey {
	if o._restored && !o.idIsLoaded {
		panic("ID was not selected in the last query and has not been set, and so is not valid")
	}
	return o.id
}

// IDIsLoaded returns true if the value was loaded from the database or has been set.
func (o *rootUlBase) IDIsLoaded() bool {
	return o.idIsLoaded
}

// SetID sets the value of ID in the object, to be saved later in the database using the Save() function.
// Normally you will not need to call this function, since the ID value is automatically generated by the
// database driver. Exceptions might include importing data to a new database, or correcting primary key conflicts when
// merging data.
// You cannot change a primary key for a record that has been written to the database. While SQL databases will
// allow it, NoSql databases will not. Save a copy and delete this one instead.
func (o *rootUlBase) SetID(v query.AutoPrimaryKey) {
	if o._restored {
		panic("error: Do not change a primary key for a record that has been saved. Instead, save a copy and delete the original.")
	}
	o.idIsLoaded = true
	o.idIsDirty = true
	o.id = v
}

// Name returns the value of the loaded name field in the database.
func (o *rootUlBase) Name() string {
	if o._restored && !o.nameIsLoaded {
		panic("Name was not selected in the last query and has not been set, and so is not valid")
	}
	return o.name
}

// NameIsLoaded returns true if the value was loaded from the database or has been set.
func (o *rootUlBase) NameIsLoaded() bool {
	return o.nameIsLoaded
}

// SetName sets the value of Name in the object, to be saved later in the database using the Save() function.
func (o *rootUlBase) SetName(v string) {
	if utf8.RuneCountInString(v) > RootUlNameMaxLength {
		panic("attempted to set RootUl.Name to a value larger than its maximum length in runes")
	}
	if o._restored &&
		o.nameIsLoaded && // if it was not selected, then make sure it gets set, since our end comparison won't be valid
		o.name == v {
		// no change
		return
	}

	o.nameIsLoaded = true
	o.name = v
	o.nameIsDirty = true
}

// GroLock returns the value of the loaded gro_lock field in the database.
func (o *rootUlBase) GroLock() int64 {
	if o._restored && !o.groLockIsLoaded {
		panic("GroLock was not selected in the last query and has not been set, and so is not valid")
	}
	return o.groLock
}

// GroLockIsLoaded returns true if the value was loaded from the database or has been set.
func (o *rootUlBase) GroLockIsLoaded() bool {
	return o.groLockIsLoaded
}

// GetAlias returns the value for the Alias node aliasKey that was returned in the most
// recent query.
func (o *rootUlBase) GetAlias(aliasKey string) query.AliasValue {
	if a, ok := o._aliases[aliasKey]; ok {
		return query.NewAliasValue(a)
	} else {
		panic("Alias " + aliasKey + " not found.")
	}
}

// IsNew returns true if the object will create a new record when saved.
func (o *rootUlBase) IsNew() bool {
	return !o._restored
}

// LeafUl returns the connected LeafUl object, if one was loaded.
// Otherwise, it will return nil.
func (o *rootUlBase) LeafUl() *LeafUl {
	if o.leafUl == nil {
		return nil
	}
	return o.leafUl
}

// LoadLeafUl returns the connected LeafUl object, if one was loaded.
// Otherwise, it will load a new one and return it.
func (o *rootUlBase) LoadLeafUl(ctx context.Context) (*LeafUl, error) {
	if o.leafUl != nil && o.leafUl.IsDirty() {
		panic("The LeafUl has changed. You must save it first before changing to a different one.")
	}
	var err error
	if o.leafUl == nil {
		pk := o.ID()
		o.leafUl, err = LoadLeafUlByRootUlID(ctx, pk)
	}
	return o.leafUl, err
}

// SetLeafUl associates obj with this RootUl
// through the reverse relationship in LeafUl.RootUl.
//
// The association is temporary until you call Save().
// WARNING! Since this is a non-nullable unique relationship,
// if a different LeafUl object is currently pointing to this RootUl,
// it will be deleted. Pass nil to just delete the old object.
func (o *rootUlBase) SetLeafUl(obj *LeafUl) {
	if o.leafUl != nil && o.leafUl.IsDirty() {
		panic("The LeafUl has changed. You must save it first before changing to a different one.")
	}
	o.leafUl = obj
	o.leafUlIsDirty = true
}

// LoadRootUl returns a RootUl from the database.
// selectNodes lets you provide nodes for selecting specific fields or additional fields from related tables.
// See [RootUlsBuilder.Select] for more info.
func LoadRootUl(ctx context.Context, pk query.AutoPrimaryKey, selectNodes ...query.Node) (*RootUl, error) {
	return queryRootUls(ctx).
		Where(op.Equal(node.RootUl().ID(), pk)).
		Select(selectNodes...).
		Get()
}

// HasRootUl returns true if a RootUl with the given primary key exists in the database.
// doc: type=RootUl
func HasRootUl(ctx context.Context, pk query.AutoPrimaryKey) (bool, error) {
	v, err := queryRootUls(ctx).
		Where(op.Equal(node.RootUl().ID(), pk)).
		Count()
	return v > 0, err
}

// The RootUlBuilder uses a builder pattern to create a query on the database.
// Create a RootUlBuilder by calling QueryRootUls, which will select all
// the RootUl object in the database. Then filter and arrange those objects
// by calling Where, Select, etc.
// End a query by calling either Load, LoadI, LoadCursor, Get, or Count.
// A RootUlBuilder stores the context it will use to perform the query, and thus is
// meant to be a short-lived object. You should not save it for later use.
type RootUlBuilder struct {
	builder *query.Builder
	ctx     context.Context
}

func newRootUlBuilder(ctx context.Context) *RootUlBuilder {
	b := RootUlBuilder{
		builder: query.NewBuilder(node.RootUl()),
		ctx:     ctx,
	}
	return &b
}

// Load terminates the query builder, performs the query, and returns a slice of RootUl objects.
// If there are any errors, nil is returned and the specific error is stored in the context.
// If no results come back from the query, it will return a non-nil empty slice.
func (b *RootUlBuilder) Load() (rootUls []*RootUl, err error) {
	b.builder.Command = query.BuilderCommandLoad
	database := db.GetDatabase("goradd_unit")
	var results any

	ctx := b.ctx
	results, err = database.BuilderQuery(ctx, b.builder)
	if results == nil || err != nil {
		return
	}
	for _, item := range results.([]map[string]any) {
		o := new(RootUl)
		o.unpack(item, o)
		rootUls = append(rootUls, o)
	}
	return
}

// LoadI terminates the query builder, performs the query, and returns a slice of interfaces.
// This can then satisfy a variety of interfaces that load arrays of objects, including KeyLabeler.
// If there are any errors, nil is returned and the specific error is stored in the context.
// If no results come back from the query, it will return a non-nil empty slice.
func (b *RootUlBuilder) LoadI() (rootUls []query.OrmObj, err error) {
	b.builder.Command = query.BuilderCommandLoad
	database := db.GetDatabase("goradd_unit")
	var results any

	ctx := b.ctx
	results, err = database.BuilderQuery(ctx, b.builder)
	if results == nil || err != nil {
		return
	}
	for _, item := range results.([]map[string]any) {
		o := new(RootUl)
		o.unpack(item, o)
		rootUls = append(rootUls, o)
	}
	return
}

// LoadCursor terminates the query builder, performs the query, and returns a cursor to the query.
//
// A query cursor is useful for dealing with large amounts of query results. However, there are some
// limitations to its use. When working with SQL databases, you cannot use a cursor while querying
// many-to-many or reverse relationships that will create an array of values.
//
// Call Next() on the returned cursor object to step through the results. Make sure you call Close
// on the cursor object when you are done. You should use
//
//	defer cursor.Close()
//
// to make sure the cursor gets closed.
func (b *RootUlBuilder) LoadCursor() (rootUlsCursor, error) {
	b.builder.Command = query.BuilderCommandLoadCursor
	database := db.GetDatabase("goradd_unit")
	result, err := database.BuilderQuery(b.ctx, b.builder)
	var cursor query.CursorI
	if result != nil {
		cursor = result.(query.CursorI)
	}
	return rootUlsCursor{cursor}, err
}

type rootUlsCursor struct {
	query.CursorI
}

// Next returns the current RootUl object and moves the cursor to the next one.
//
// If there are no more records, it returns nil.
func (c rootUlsCursor) Next() (*RootUl, error) {
	if c.CursorI == nil {
		return nil, nil
	}

	row, err := c.CursorI.Next()
	if row == nil || err != nil {
		return nil, err
	}
	o := new(RootUl)
	o.unpack(row, o)
	return o, nil
}

// Get is a convenience method to return only the first item found in a query.
// The entire query is performed, so you should generally use this only if you know
// you are selecting on one or very few items.
// If an error occurs, or no results are found, a nil is returned.
func (b *RootUlBuilder) Get() (*RootUl, error) {
	results, err := b.Load()
	if err != nil || len(results) == 0 {
		return nil, err
	}
	return results[0], nil
}

// Where adds a condition to filter what gets selected.
// Calling Where multiple times will AND the conditions together.
func (b *RootUlBuilder) Where(c query.Node) *RootUlBuilder {
	b.builder.Where(c)
	return b
}

// OrderBy specifies how the resulting data should be sorted.
// By default, the given nodes are sorted in ascending order.
// Add Descending() to the node to specify that it should be sorted in descending order.
func (b *RootUlBuilder) OrderBy(nodes ...query.Sorter) *RootUlBuilder {
	b.builder.OrderBy(nodes...)
	return b
}

// Limit will return a subset of the data, limited to the offset and number of rows specified.
// For large data sets and specific types of queries, this can be slow, because it will perform
// the entire query before computing the limit.
// You cannot limit a query that has embedded arrays.
func (b *RootUlBuilder) Limit(maxRowCount int, offset int) *RootUlBuilder {
	b.builder.Limit(maxRowCount, offset)
	return b
}

// Select specifies what specific columns will be loaded with data.
// By default, all the columns of the root_ul table will be queried and loaded.
// If nodes contains columns from the root_ul table, that will limit the columns queried and loaded to only those columns.
// If related tables are specified, then all the columns from those tables are queried, selected and joined to the result.
// If columns in related tables are specified, then only those columns will be queried and loaded.
// Depending on the query, additional columns may automatically be added to the query. In particular, primary key columns
// will be added in most situations. The exception to this would be in distinct queries, group by queries, or subqueries.
func (b *RootUlBuilder) Select(nodes ...query.Node) *RootUlBuilder {
	b.builder.Select(nodes...)
	return b
}

// Calculation adds operation as an aliased value onto base.
// After the query, you can read the data by passing alias to GetAlias on the returned object.
func (b *RootUlBuilder) Calculation(base query.TableNodeI, alias string, operation query.OperationNodeI) *RootUlBuilder {
	b.builder.Calculation(base, alias, operation)
	return b
}

// Distinct removes duplicates from the results of the query.
// Adding a Select() is usually required.
func (b *RootUlBuilder) Distinct() *RootUlBuilder {
	b.builder.Distinct()
	return b
}

// GroupBy controls how results are grouped when using aggregate functions with Calculation.
func (b *RootUlBuilder) GroupBy(nodes ...query.Node) *RootUlBuilder {
	b.builder.GroupBy(nodes...)
	return b
}

// Having does additional filtering on the results of the query after the query is performed.
func (b *RootUlBuilder) Having(node query.Node) *RootUlBuilder {
	b.builder.Having(node)
	return b
}

// Count terminates a query and returns just the number of items in the result.
// If you have Select or Calculation columns in the query, it will count NULL results as well.
// To not count NULL values, use Where in the builder with a NotNull operation.
// To count distinct combinations of items, call Distinct() on the builder.
func (b *RootUlBuilder) Count() (int, error) {
	b.builder.Command = query.BuilderCommandCount
	database := db.GetDatabase("goradd_unit")

	ctx := b.ctx
	results, err := database.BuilderQuery(ctx, b.builder)
	if results == nil || err != nil {
		return 0, err
	}
	return results.(int), nil
}

// CountRootUls returns the total number of items in the root_ul table.
func CountRootUls(ctx context.Context) (int, error) {
	return QueryRootUls(ctx).Count()
}

// unpack recursively transforms data coming from the database into ORM objects.
func (o *rootUlBase) unpack(m map[string]interface{}, objThis *RootUl) {

	if v, ok := m["id"]; ok && v != nil {
		if o.id, ok = v.(query.AutoPrimaryKey); ok {
			o.idIsLoaded = true
			o.idIsDirty = false
			o._originalPK = o.id
		} else {
			panic("Wrong type found for id.")
		}
	} else {
		o.idIsLoaded = false
		o.id = query.TempAutoPrimaryKey()
		o.idIsDirty = false
	}

	if v, ok := m["name"]; ok && v != nil {
		if o.name, ok = v.(string); ok {
			o.nameIsLoaded = true
			o.nameIsDirty = false
		} else {
			panic("Wrong type found for name.")
		}
	} else {
		o.nameIsLoaded = false
		o.name = ""
		o.nameIsDirty = false
	}

	if v, ok := m["groLock"]; ok && v != nil {
		if o.groLock, ok = v.(int64); ok {
			o.groLockIsLoaded = true
		} else {
			panic("Wrong type found for groLock.")
		}
	} else {
		o.groLockIsLoaded = false
		o.groLock = 0
	}

	// Reverse references

	if v, ok := m["leafUl"]; ok {
		if v2, ok2 := v.(map[string]any); ok2 {
			o.leafUl = new(LeafUl)
			o.leafUl.unpack(v2, o.leafUl)
			o.leafUlIsDirty = false
		} else {
			panic("Wrong type found for leafUl object.")
		}
	} else {
		o.leafUl = nil
		o.leafUlIsDirty = false
	}

	if v, ok := m["aliases_"]; ok {
		o._aliases = v.(map[string]any)
	}

	o._restored = true

}

// save will update or insert the object, depending on the state of the object.
func (o *rootUlBase) save(ctx context.Context) error {
	if o._restored {
		return o.update(ctx)
	} else {
		return o.insert(ctx)
	}
}

// update will update the values in the database, saving any changed values.
// If the table has auto-generated values, those will be updated automatically.
func (o *rootUlBase) update(ctx context.Context) error {
	if !o._restored {
		panic("cannot update a record that was not originally read from the database.")
	}
	if !o.IsDirty() {
		return nil // nothing to save
	}

	var modifiedFields map[string]interface{}

	d := Database()
	err := db.WithTransaction(ctx, d, func(ctx context.Context) error {

		modifiedFields = getRootUlUpdateFields(o)
		if len(modifiedFields) != 0 {
			err2 := d.Update(ctx, "root_ul",
				map[string]any{
					"id": o._originalPK,
				},
				modifiedFields,
				"gro_lock",
				o.GroLock(),
			)
			if err2 != nil {
				return err2
			}
		}

		if o.leafUlIsDirty {
			// relation connection changed

			// Since the other side of the relationship cannot be null, if there is an object already attached
			// that is different than the one we are trying to attach, we will panic to warn the developer.
			if oldObj, err := QueryLeafUls(ctx).
				Where(op.Equal(node.LeafUl().RootUlID(), o.ID())).
				Select(node.LeafUl().RootUlID()).
				Select(node.LeafUl().GroLock()).
				Get(); err != nil {
				return err
			} else if oldObj != nil {
				if o.leafUl == nil || oldObj.PrimaryKey() != o.leafUl.PrimaryKey() {
					// Delete the old object
					err = oldObj.Delete(ctx)
					if err != nil {
						return err
					}
				}
			}
			// we are moving the attachment from another object to our object, or attaching an object that is already attached.
			if o.leafUl != nil {
				o.leafUl.SetRootUlID(o.PrimaryKey())
				if err := o.leafUl.Save(ctx); err != nil {
					return err
				}
			}

		} else {

			// save related object in case internal values changed
			if o.leafUl != nil {
				if err := o.leafUl.Save(ctx); err != nil {
					return err
				}
			}
		}

		return nil
	}) // transaction
	if err != nil {
		return err
	}
	// update generated lock value
	if l, ok := modifiedFields["gro_lock"]; ok {
		o.groLock = l.(int64)
		o.groLockIsLoaded = true
	}

	o.resetDirtyStatus()
	if len(modifiedFields) != 0 {
		broadcast.Update(ctx, "goradd_unit", "root_ul", o._originalPK, anyutil.SortedKeys(modifiedFields)...)
	}

	return nil
}

// insert will insert the object into the database. Related items will be saved.
func (o *rootUlBase) insert(ctx context.Context) (err error) {
	var insertFields map[string]interface{}
	d := Database()
	err = db.WithTransaction(ctx, d, func(ctx context.Context) error {
		if !o.nameIsLoaded {
			panic("a value for Name is required, and there is no default value. Call SetName() before inserting the record.")
		}
		insertFields = getRootUlInsertFields(o)
		err = d.Insert(ctx, "root_ul", insertFields, "id")
		if err != nil {
			return err
		}
		o.id = insertFields["id"].(query.AutoPrimaryKey)
		o._originalPK = o.id
		o.idIsLoaded = true

		if o.leafUl != nil {
			o.leafUl.SetRootUlID(o.PrimaryKey())
			if err = o.leafUl.Save(ctx); err != nil {
				return err
			}
		}

		return nil

	}) // transaction

	if err != nil {
		return
	}
	if t, ok := insertFields["gro_lock"]; ok {
		o.groLock = t.(int64)
		o.groLockIsLoaded = true
	}

	o.resetDirtyStatus()
	o._restored = true
	broadcast.Insert(ctx, "goradd_unit", "root_ul", o.PrimaryKey())
	return
}

// getUpdateFields returns the database columns that will be sent to the update process.
// This will include timestamp fields only if some other column has changed.
func (o *rootUlBase) getUpdateFields() (fields map[string]interface{}) {
	fields = map[string]interface{}{}
	if o.idIsDirty {
		fields["id"] = o.id
	}
	if o.nameIsDirty {
		fields["name"] = o.name
	}
	return
}

// getInsertFields returns the fields that will be specified in an insert operation.
// Optional fields that have not been set and have no default will be returned as nil.
// NoSql databases should interpret this as no value. Sql databases should interpret this as
// explicitly setting a NULL value, which would override any database specific default value.
// Auto-generated fields will be returned here with their generated values, except AutoPK fields, which are returned
// as a new AutoPrimaryKey to indicate that the driver will fill this in after the insert.
func (o *rootUlBase) getInsertFields() (fields map[string]interface{}) {
	fields = map[string]interface{}{}
	if o.idIsDirty {
		fields["id"] = o.id
	}

	fields["name"] = o.name
	fields["gro_lock"] = db.RecordVersion(0)
	return
}

// Delete deletes the record from the database.
//
// An associated {= rev.ReverseIdentifier  will also be deleted since its RootUl field is not nullable.
func (o *rootUlBase) Delete(ctx context.Context) (err error) {
	if o == nil {
		return // allow deleting of a nil object to be a noop
	}
	if !o._restored {
		panic("Cannot delete a record that has no primary key value.")
	}
	d := Database()
	err = db.WithTransaction(ctx, d, func(ctx context.Context) error {

		{
			obj, err := QueryLeafUls(ctx).
				Where(op.Equal(node.LeafUl().RootUl().PrimaryKey(), o._originalPK)).
				Get()
			if err != nil {
				return err
			}
			if obj != nil {
				if err = obj.Delete(ctx); err != nil {
					return err
				}
			}
			// Set this object's pointer to the reverse object to nil to mark that we broke the link
			o.leafUl = nil
		}

		return d.Delete(ctx, "root_ul",
			map[string]any{"id": o._originalPK},
			"gro_lock",
			o.GroLock(),
		)
	})

	if err != nil {
		return err
	}
	broadcast.Delete(ctx, "goradd_unit", "root_ul", o._originalPK)
	return
}

// deleteRootUl deletes the RootUl with primary key pk from the database
// and handles associated records.
func deleteRootUl(ctx context.Context, pk query.AutoPrimaryKey) error {
	d := db.GetDatabase("goradd_unit")
	err := db.WithTransaction(ctx, d, func(ctx context.Context) error {
		if obj, err := LoadRootUl(ctx,
			pk,
			node.RootUl().ID(),
			node.RootUl().GroLock(),
		); err != nil {
			return err
		} else if obj == nil {
			return db.NewRecordNotFoundError("root_ul", pk)
		} else {
			if err := obj.Delete(ctx); err != nil {
				return err
			}
		}
		return nil
	})
	return err
}

// resetDirtyStatus resets the dirty status of every field in the object.
func (o *rootUlBase) resetDirtyStatus() {
	o.idIsDirty = false
	o.nameIsDirty = false
	o.leafUlIsDirty = false

}

// IsDirty returns true if the object has been changed since it was read from the database or created.
func (o *rootUlBase) IsDirty() (dirty bool) {
	dirty = o.idIsDirty ||
		o.nameIsDirty

	dirty = dirty ||
		o.leafUlIsDirty

	dirty = dirty || (o.leafUl != nil && o.leafUl.IsDirty())

	return
}

// Get returns the value of a field in the object based on the field's name.
// It will also get related objects if they are loaded.
// Invalid fields and objects are returned as nil.
// Get can be used to retrieve a value by using the Field() of a node.
func (o *rootUlBase) Get(key string) interface{} {
	switch key {
	case RootUlIDField:
		if !o.idIsLoaded {
			return nil
		}
		return o.id
	case RootUlNameField:
		if !o.nameIsLoaded {
			return nil
		}
		return o.name
	case RootUlGroLockField:
		if !o.groLockIsLoaded {
			return nil
		}
		return o.groLock
	case RootUlLeafUlField:
		return o.leafUl
	}
	return nil
}

// MarshalBinary serializes the object into a buffer that is deserializable using UnmarshalBinary.
// It should be used for transmitting database objects over the wire, or for temporary storage. It does not send
// a version number, so if the data format changes, its up to you to invalidate the old stored objects.
// The framework uses this to serialize the object when it is stored in a control.
func (o *rootUlBase) MarshalBinary() ([]byte, error) {
	buf := new(bytes.Buffer)
	enc := gob.NewEncoder(buf)
	if err := o.encodeTo(enc); err != nil {
		return nil, err
	}
	return buf.Bytes(), nil
}

func (o *rootUlBase) encodeTo(enc db.Encoder) error {

	if err := enc.Encode(o.id); err != nil {
		return fmt.Errorf("error encoding RootUl.id: %w", err)
	}
	if err := enc.Encode(o.idIsLoaded); err != nil {
		return fmt.Errorf("error encoding RootUl.idIsLoaded: %w", err)
	}
	if err := enc.Encode(o.idIsDirty); err != nil {
		return fmt.Errorf("error encoding RootUl.idIsDirty: %w", err)
	}

	if err := enc.Encode(o.name); err != nil {
		return fmt.Errorf("error encoding RootUl.name: %w", err)
	}
	if err := enc.Encode(o.nameIsLoaded); err != nil {
		return fmt.Errorf("error encoding RootUl.nameIsLoaded: %w", err)
	}
	if err := enc.Encode(o.nameIsDirty); err != nil {
		return fmt.Errorf("error encoding RootUl.nameIsDirty: %w", err)
	}

	if err := enc.Encode(o.groLock); err != nil {
		return fmt.Errorf("error encoding RootUl.groLock: %w", err)
	}
	if err := enc.Encode(o.groLockIsLoaded); err != nil {
		return fmt.Errorf("error encoding RootUl.groLockIsLoaded: %w", err)
	}

	if o.leafUl == nil {
		if err := enc.Encode(false); err != nil {
			return err
		}
	} else {
		if err := enc.Encode(true); err != nil {
			return err
		}
		if err := enc.Encode(o.leafUl); err != nil {
			return fmt.Errorf("error encoding RootUl.leafUl: %w", err)
		}
	}

	if err := enc.Encode(o.leafUlIsDirty); err != nil {
		return fmt.Errorf("error encoding RootUl.leafUlIsDirty: %w", err)
	}

	if o._aliases == nil {
		if err := enc.Encode(false); err != nil {
			return err
		}
	} else {
		if err := enc.Encode(true); err != nil {
			return err
		}
		if err := enc.Encode(o._aliases); err != nil {
			return fmt.Errorf("error encoding RootUl._aliases: %w", err)
		}
	}

	if err := enc.Encode(o._restored); err != nil {
		return fmt.Errorf("error encoding RootUl._restored: %w", err)
	}
	if err := enc.Encode(o._originalPK); err != nil {
		return fmt.Errorf("error encoding RootUl._originalPK: %w", err)
	}
	return nil
}

// UnmarshalBinary converts a structure that was created with MarshalBinary into a RootUl object.
func (o *rootUlBase) UnmarshalBinary(data []byte) (err error) {
	buf := bytes.NewReader(data)
	dec := gob.NewDecoder(buf)
	return o.decodeFrom(dec)
}

func (o *rootUlBase) decodeFrom(dec db.Decoder) (err error) {
	var isPtr bool

	_ = isPtr
	if err = dec.Decode(&o.id); err != nil {
		return fmt.Errorf("error decoding RootUl.id: %w", err)
	}
	if err = dec.Decode(&o.idIsLoaded); err != nil {
		return fmt.Errorf("error decoding RootUl.idIsLoaded: %w", err)
	}
	if err = dec.Decode(&o.idIsDirty); err != nil {
		return fmt.Errorf("error decoding RootUl.idIsDirty: %w", err)
	}

	if err = dec.Decode(&o.name); err != nil {
		return fmt.Errorf("error decoding RootUl.name: %w", err)
	}
	if err = dec.Decode(&o.nameIsLoaded); err != nil {
		return fmt.Errorf("error decoding RootUl.nameIsLoaded: %w", err)
	}
	if err = dec.Decode(&o.nameIsDirty); err != nil {
		return fmt.Errorf("error decoding RootUl.nameIsDirty: %w", err)
	}

	if err = dec.Decode(&o.groLock); err != nil {
		return fmt.Errorf("error decoding RootUl.groLock: %w", err)
	}
	if err = dec.Decode(&o.groLockIsLoaded); err != nil {
		return fmt.Errorf("error decoding RootUl.groLockIsLoaded: %w", err)
	}

	if err = dec.Decode(&isPtr); err != nil {
		return fmt.Errorf("error decoding RootUl.leafUl isPtr: %w", err)
	}
	if isPtr {
		if err = dec.Decode(&o.leafUl); err != nil {
			return fmt.Errorf("error decoding RootUl.leafUl: %w", err)
		}
	}

	if err = dec.Decode(&o.leafUlIsDirty); err != nil {
		return fmt.Errorf("error decoding RootUl.leafUlIsDirty: %w", err)
	}
	if err = dec.Decode(&isPtr); err != nil {
		return fmt.Errorf("error decoding RootUl._aliases isPtr: %w", err)
	}
	if isPtr {
		if err = dec.Decode(&o._aliases); err != nil {
			return fmt.Errorf("error decoding RootUl._aliases: %w", err)
		}
	}

	if err = dec.Decode(&o._restored); err != nil {
		return fmt.Errorf("error decoding RootUl._restored: %w", err)
	}
	if err = dec.Decode(&o._originalPK); err != nil {
		return fmt.Errorf("error decoding RootUl._originalPK: %w", err)
	}
	return
}

// MarshalJSON serializes the object into a JSON object.
// Only valid data will be serialized, meaning, you can control what gets serialized by using Select to
// select only the fields you want when you query for the object. Another way to control the output
// is to call MarshalStringMap, modify the map, then encode the map.
func (o *rootUlBase) MarshalJSON() (data []byte, err error) {
	v := o.MarshalStringMap()
	return json.Marshal(v)
}

// MarshalStringMap serializes the object into a string map of interfaces.
// Only valid data will be serialized, meaning, you can control what gets serialized by using Select to
// select only the fields you want when you query for the object. The keys are the same as the json keys.
func (o *rootUlBase) MarshalStringMap() map[string]interface{} {
	v := make(map[string]interface{})

	if o.idIsLoaded {
		v["id"] = o.id
	}

	if o.nameIsLoaded {
		v["name"] = o.name
	}

	if o.groLockIsLoaded {
		v["groLock"] = o.groLock
	}

	if obj := o.leafUl; obj != nil {
		v["leafUl"] = obj.MarshalStringMap()
	}
	for _k, _v := range o._aliases {
		v[_k] = _v
	}
	return v
}

// UnmarshalJSON unmarshalls the given json data into the RootUl. The RootUl can be a
// newly created object, or one loaded from the database.
//
// After unmarshalling, the object is not  saved. You must call Save to insert it into the database
// or update it.
//
// Unmarshalling of sub-objects, as in objects linked via foreign keys, is not currently supported.
//
// The fields it expects are:
//
//	"id" - query.AutoPrimaryKey
//	"name" - string
//	"groLock" - int64
func (o *rootUlBase) UnmarshalJSON(data []byte) (err error) {
	var v map[string]interface{}
	if len(data) == 0 {
		return
	}
	d := json.NewDecoder(bytes.NewReader(data))
	d.UseNumber() // use a number to avoid precision errors
	if err = d.Decode(&v); err != nil {
		return err
	}
	return o.UnmarshalStringMap(v)
}

// UnmarshalStringMap will load the values from the stringmap into the object.
//
// Override this in RootUl to modify the json before sending it here.
func (o *rootUlBase) UnmarshalStringMap(m map[string]interface{}) (err error) {
	for k, v := range m {
		switch k {

		case "id":
			{
				if v == nil {
					return fmt.Errorf("field %s cannot be null", k)
				}

				if u, ok := Database().(db.AutoPrimaryKeyJsonUnmarshaller); ok {
					u.AutoPrimaryKeyJsonUnmarshal(v)
				} else {
					switch n := v.(type) {
					case json.Number:
						n2, err := n.Int64()
						if err != nil {
							return err
						}
						o.SetID(query.NewAutoPrimaryKey(n2))
					default:
						o.SetID(query.NewAutoPrimaryKey(v))
					}
				}
			}
		case "name":
			{
				if v == nil {
					return fmt.Errorf("field %s cannot be null", k)
				}

				if s, ok := v.(string); !ok {
					return fmt.Errorf("json field %s must be a string", k)
				} else {
					o.SetName(s)
				}
			}
		case "leafUl":
			v2 := NewLeafUl()
			m2, ok := v.(map[string]any)
			if !ok {
				return fmt.Errorf("json field %s must be a map", k)
			}
			err = v2.UnmarshalStringMap(m2)
			if err != nil {
				return
			}
			o.SetLeafUl(v2)

		}
	}
	return
}
