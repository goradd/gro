
{{g
//*** {{includeName}}
}}
{{
// resetDirtyStatus resets the dirty status of every field in the object.
func (o *{{= table.DecapIdentifier}}Base) resetDirtyStatus() {
{{for _,col := range table.SettableColumns() }}
	o.{{= col.Field }}IsDirty = false
{{for}}
{{for _,ref := range table.ReverseReferences }}
	o.{{= ref.ReverseField }}IsDirty = false
{{for}}
{{for _,mm := range table.ManyManyReferences }}
	o.{{= mm.Field }}IsDirty = false
	o.{{= mm.Field }}Pks = nil
{{for}}

}

// IsDirty returns true if the object has been changed since it was read from the database or created.
func (o *{{= table.DecapIdentifier}}Base) IsDirty() (dirty bool) {
    dirty = {{join table.SettableColumns(), " ||\n"}}o.{{= _j.Field }}IsDirty{{join}}

{{if table.HasReferences() }}
    dirty = dirty ||
        {{join table.References, " ||\n"}}o.{{= _j.Field }} != nil && o.{{= _j.Field }}.IsDirty()){{join}}
{{if}}

{{if table.HasReferences() }}
	dirty = dirty ||
	    {{join table.ReverseReferences, "|| \n"}}o.{{= _j.ReverseField }}IsDirty{{join}}

{{for _,ref := range table.ReverseReferences }}
{{if ref.IsUnique }}
    dirty = dirty || (o.{{= ref.ReverseField }} != nil && o.{{= ref.ReverseField }}.IsDirty())
{{else}}
    for obj := range o.{{= ref.ReverseField }}.ValuesIter() {
        dirty = dirty || obj.IsDirty()
    }
{{if}}
{{for}}
{{if}}

{{if len(table.ManyManyReferences) > 0}}
	dirty = dirty ||
	    {{join table.ManyManyReferences, "|| \n"}}o.{{= _j.Field }}IsDirty{{join}}

    {{for _,mm := range table.ManyManyReferences}}

    for obj := range o.{{= mm.Field }}.ValuesIter() {
        dirty = dirty || obj.IsDirty()
    }
    {{for}}
{{if}}

    return
}

}}
