
{{g
//*** {{includeName}}
}}
{{
    if err := enc.Encode(&o.{{= rev.ReverseField }} ); err != nil {
        return err
    }

    if err := enc.Encode(o.{{= rev.ReverseField }}IsDirty); err != nil {
        return err
    }

    {{# We will need to rebuild the map based on the object decoded, since they are pointers to the same objects}}
}}
